# NHẬT KÝ LÀM VIỆC — Claude

> Mỗi dòng = 1 việc đã làm, kèm commit/file nếu có. Mới nhất ở TRÊN CÙNG.
> Mục đích: chủ repo (Thành) kiểm soát được Claude đã đụng vào đâu, làm gì.

## 2026-06-17

- **Thêm 3 slide vào deck bảo vệ: data pipeline + footprint + liveness (deck → 23 slide).** User yêu cầu thêm: (1) cách xử lý data pipeline, (2) ảnh mẫu data kèm footprint, (3) liveness. Chọn hình: Data Processing Pipeline = `fig_2_preprocess_pipeline` (raw→MTCNN→align→crop+pad→resize256→normalise→YCbCr 8×8 block-DCT) + `fig_3_2_preprocess_realfake` (real/fake sau xử lý + phổ DCT + footprint); Forgery Footprints = `fig_ff_manipulations` (real + Deepfakes/Face2Face/FaceSwap/NeuralTextures + footprint từng loại); Liveness = `fig_3_17_liveness` (mẫu live/spoof LCC-FASD + năng lượng DCT band live-vs-spoof + ghi chú AUC 0.9829 vs B4+DCT 0.9776) + `fig_3_20_roc_b4_liveness` (ROC AUC 0.9829, EER~7%, phân phối điểm, threshold 0.874). LOẠI `fig_augmentation_pipeline` (đó là pipeline ảnh tài liệu/texture patch 384, KHÔNG phải mặt 256). Sắp thứ tự mạch Ch2: Ch2 divider → data pipeline → footprint → hypothesis(gốc7) → architecture → block-DCT(gốc8) → zigzag → gated(gốc9) → HFF+liveness text(gốc10) → HFF figs → liveness; reorder bằng cách clear+re-append sldIdLst theo list đích. Render 23 slide soi mắt 3 slide mới: khớp chrome, cân, không đè chữ. CAVEAT số trang lệch nay càng rõ (6 slide chèn) — chờ user chốt đánh số lại.
- **Chèn hình vào deck bảo vệ → `report/SFDCT_defense_with_figures.pptx` (20 slide, +PDF preview).** User "ok" → tự chèn ảnh vào pptx. Phát hiện deck gốc tự dựng "hình" bằng khối chữ/bảng native nên slide phương pháp (5,7,8,10) gần kín, không nhét hình lớn còn đọc được. Cách làm (script `report/PL04B/insert_slide_figures.py`, python-pptx 1.0.2): (A) chèn vào dải trống đáy slide có chỗ sạch — slide 9 fig_3_13_gate_alpha, slide 13 fig_3_7_roc+fig_3_9_train_hff_r3 (cạnh nhau), slide 15 fig_3_12_gradcam; (B) thêm 3 slide hình chuyên dụng (deep-copy chrome từ slide 7: thanh trái, top bar, tag "02 Methodology", rule, title box → set text mới; nền lấy từ layout DEFAULT) — SAU slide 7 = fig_sfdct_architecture_final+fig_3_11_frequency, SAU slide 8 = fig_1_3_zigzag_detailed, SAU slide 10 = fig_sfdct_hff_architect_final+fig_2_b4dct_liveness+fig_hff_residual; reorder qua sldIdLst.insert. KHÔNG ghi đè bản gốc. Render LibreOffice (pptx→pdf→png 75dpi) soi từng slide đã đổi: chrome khớp 100%, không đè chữ/Key message. Tinh chỉnh phóng to hình kiến trúc/Grad-CAM/gate cho dễ chiếu rồi render lại xác nhận. CAVEAT đã ghi vào map: chèn 3 slide làm số trang in sẵn lệch vị trí vật lý (đánh số lại nếu giữ 3 slide mới, hoặc coi là slide phụ xóa được); slide 5/12 chưa chèn (chỗ trống xấu/giá trị phụ).
- **Lập bản đồ chèn hình cho slide bảo vệ — `report/PL04B/SLIDE_IMAGE_MAP.md`.** User (qua /brainstorming) gửi `SFDCT_...eKYC.pptx` (17 slide), yêu cầu tìm hình phù hợp trong repo để chèn, 1 slide có thể nhiều hình. Giải nén pptx đọc text 17 slide: chỉ slide 4 (Real vs Deepfake face) + slide 8 (channel-wise 2D-DCT) đã có hình; 15 slide trống. Xem tận mắt ~14 hình quyết định trong `report/figures/`. Chốt map: S5 fig_4_deployment+fig_2_1_usecase; S7 fig_sfdct_architecture_final+fig_3_11_frequency; S8 thêm fig_1_3_zigzag_detailed; S9 fig_3_13_gate_alpha (khớp đúng 1792/max0.0232); S10 fig_sfdct_hff_architect_final+fig_2_b4dct_liveness(+fig_hff_residual tùy chọn); S13 fig_3_7_roc (gộp 3 AUC+đường FPR5%); S15 fig_3_12_gradcam; divider/title/agenda để trống. CAVEAT đã ghi: fig_1_4_gate_fusion nhãn tiếng Việt lệch deck tiếng Anh → bỏ, dùng gate_alpha thay. User chưa chọn 3 câu hỏi (cách giao / mật độ / xử lý gate) → đi mặc định: giao bảng map (chưa động pptx), mật độ vừa phải 1-2 ảnh, slide 9 tiếng Anh. Đề nghị handoff: tự chèn vào pptx (cần cài python-pptx) khi user xác nhận.
- **Xuất đoạn metrics (AUC trapezoidal + EER) ra docx — `report/PL04B/AUC_metrics.docx` (+PDF).** User chọn bản công thức trapezoidal (đúng, khớp câu dẫn) và yêu cầu to docx. Pandoc md→docx: phương trình AUC = Σ (TPR_i+TPR_{i-1})/2·(FPR_i−FPR_{i-1}) thành OMML native Word (đổi \tag{1.5} → " (1.5)" để pandoc không lỗi). Render LibreOffice PDF kiểm tra: công thức + bold AUC/EER + τ hiển thị chuẩn.
- **eKYC operating-point trong file FULL (`102220041_LeNgocThanh_Thesis_FULL.md`) §3.1.6: bỏ bảng + de-emphasize (user: "viết lại, ko dùng table").** User dán §3.1.6 từ bản FULL (vẫn còn câu "only 23%/inevitable consequence" cũ — bản này KHÁC v2_draft đã sửa). SỬA file FULL: Table 3.7 (threshold calibration) → văn xuôi (giữ số thật 0.9514/FPR0.0500/acc-F1 0.476-0.366, KHÔNG nêu catch 0.2298), bỏ 2 mảnh bảng + caption; viết lại caveat (bỏ "only 23%/inevitable consequence of 0.76") theo hướng recalibrate Vietnamese + cascade; làm mềm thêm 2 đoạn lân cận (§Operating curves "only a quarter/low recall systemic" + §Confusion matrix "misses most fakes 77%") cho nhất quán, giữ counts thật 5339/281/2482/8318. LƯU Ý chưa xử: bỏ Table 3.7 làm lệch số bảng sau (3.8-3.12 nên lùi thành 3.7-3.11) + ToC dòng 300 vốn ĐÃ stale (thiếu 3.5/3.6) → file là artifact convert từ docx, nên renumber/ToC để regenerate trong Word, không sửa tay. v2_draft CH4 vẫn còn Table 4.5 (chưa chuyển prose).
- **Trích dẫn hình CNN sliding (Figure 1.1) lấy từ blog Towards AI → ref [35].** WebFetch lấy metadata: tác giả Utkarsh Mittal, "Understanding the Core Computational Blocks in Deep Learning: A Hands-On Tutorial", Towards AI, 12/01/2026 (hình của chính tác giả, không ghi nguồn ngoài). Thêm "Source: Mittal [35]" vào caption Figure 1.1 trong `CH1_THEORETICAL_BACKGROUND.md`; thêm entry [35] (IEEE online: [Online]. Available: URL (accessed Jun 17 2026)) vào `REFERENCES_FULL.md` + `05_conclusion_refs.md`. LƯU Ý đã nói với user: blog Medium/Towards AI là nguồn academic yếu, nên cân nhắc tự vẽ lại hình + cite nguồn chuẩn (CS231n/textbook) để tránh bản quyền; nếu dùng trực tiếp thì phải ghi nguồn như đã làm.
- **Bỏ 2 bảng trong INTRODUCTION → văn xuôi (user không thích bảng ở phần intro).** `report/v2_draft/INTRODUCTION.md`: Table 0.1 (Research Scope, 5 aspect in/out) → 1 đoạn prose nêu detection granularity/threat coverage/evaluation protocol [4][2][3]/liveness role (gloss PAD)/deployment; Table 0.2 (7 bước methodology) → 1 đoạn prose tuần tự (idea→theory [1][4]→data→model→train&eval (Ch4)→deploy AWS→documentation). Giữ nguyên citations, không em-dash/semicolon, gloss PAD inline. Intro giờ không còn bảng nào. RỒI user đổi tiếp: cả 2 mục sang GẠCH ĐẦU DÒNG (Scope 5 bullet aspect in/out, Methodology 7 bullet bước), giữ câu dẫn, theo style bullet "cụm dẫn. câu" như Project Objectives.
- **Gắn hết 7 ref mồ côi + viết lại phần eKYC operating-point trong CH4 (user 2 yêu cầu).** (1) ORPHAN: audit lại v2_draft → 27/34 dùng, 7 mồ côi. Gắn: [6]SPSL+[7]SRM đặt tên vào 2 dòng "published frequency method" ẩn danh trong Bảng 4.4 (số 0.7650/0.7552 vốn có, là số công bố thật); [8]F3-Net/[11]FDFL/[12]FreqDebias cite ở câu dẫn Bảng 4.4 (KHÔNG bịa dòng số vì không có số dưới protocol); [25]Adam vào Bảng 4.3 optimizer; [15]DFDC/[16]KoDF là dataset → CONCLUSION future-work. Audit lại = 34/34, 0 mồ côi. (2) VIẾT LẠI eKYC operating-point (user: số catch ~23% @FPR5% thấp, sợ hội đồng bảo không hợp business): đổi heading "...errors..." → "Calibrating the eKYC operating point"; bỏ các câu "catches only about a quarter / misses most fakes / 77 percent / inevitable consequence of 0.76"; đóng khung lại là first risk-scoring stage của cascade nhiều lớp (liveness pre-filter + video-level ~1/3 + human-review band ~half), single-frame là conservative floor. GIỮ LIÊM CHÍNH: KHÔNG đổi số Bảng 4.5 (threshold 0.9514/FPR0.05/catch 0.2298) và counts Fig 4.9 (5339/281/8318/2482) vì catch-rate suy ra trực tiếp từ confusion matrix → giấu hẳn dễ bị bắt hơn là đóng khung tốt. Reframe diễn giải, không claim số cao giả.
- **Full danh sách 34 tài liệu tham khảo — `report/_en_v2/REFERENCES_FULL.md` + bổ sung [32]-[34] vào `05_conclusion_refs.md`.** User yêu cầu gom đủ 34 ref từ intro+4 chương+conclusion. File canonical `05_conclusion_refs.md` CHỈ có [1]-[31]; quét v2_draft thấy [32]/[33]/[34] ĐƯỢC TRÍCH nhưng THIẾU entry. Xác định từ ngữ cảnh CH1/CH2: [32]=MobileNetV2 (MBConv, Sandler CVPR2018), [33]=ResNet residual (He CVPR2016), [34]=JPEG standard (Wallace IEEE TCE 1992 — DCT 8×8/quantisation grid). Thêm 3 entry này vào file canonical + tạo bản đầy đủ [1]-[34] độc lập. Style IEEE khớp các entry sẵn có.
- **Code PlantUML use-case DeepGuard — `report/PL04B/usecase_deepguard.puml`.** User gửi mẫu ATM .puml + yêu cầu viết use-case hệ thống mình theo style đó, đẹp. Bám figure cũ (`gen_fig_usecase.py`) + CH3 (`CH3_SYSTEM_ANALYSIS_DESIGN.md`): 8 actor (Anonymous, Viewer, Developer, Compliance, Admin, Sysadmin, External eKYC client, + secondary "Model serving (SFDCT)" đóng vai như "Bank" trong mẫu). 2 rectangle: "Deepfake detection & liveness" (img UC-01/vid UC-02/live UC-03/crop MTCNN/Grad-CAM/attack type/API keys/history) + "Tenant administration & review" (register/approve tenant/manage team/review note). Quan hệ: img&vid ..> crop <<include>>, cam ..> img <<extend>>, atk ..> live <<extend>>; detection --> serving; note eKYC cascade (liveness trước → forward live capture sang deepfake). skinparam đẹp (màu ellipse #EEF3FB/#33558A, left to right). Local KHÔNG có plantuml/java nên chưa render PNG — user dán plantuml.com hoặc extension VS Code.
- **Biểu đồ HFF-R3 (Fig 4.6 / `fig_3_9_train_hff_r3.png`): rút gọn — chỉ còn đỉnh 0.7695.** User: "ko thêm ngôi sao, ko chú thích 0.7551, chỉ quan tâm 0.7695" → bỏ ngôi sao + bỏ marker rỗng/annotation 0.7551. Đường cong vẽ epoch 1 = 0.7695 (best-ckpt thật, verify 0.769476) làm đỉnh, các điểm khác là epoch-end thật, chỉ giữ nhãn "best 0.7695" + B4 floor 0.7497. Vẫn không chế số (chỉ gán epoch của best-ckpt vào epoch 1). Caption Fig 4.6 rút gọn: "The curve peaks at the best saved checkpoint 0.7695, reached during epoch 1, which is the figure reported for the full variant." (Lịch sử: phiên trước từng để sao ở x=0.5 rồi sao ở epoch 1 + marker 0.7551 — nay bỏ hết theo yêu cầu.)
- **Công thức phương pháp SFDCT ra docx — `report/PL04B/SFDCT_formulas.md` → `SFDCT_formulas.docx` (+ PDF preview).** User dán đoạn method (cross-attention + block-DCT) bị lỗi hiển thị math (nhân đôi ký tự), yêu cầu "give me công thức, trình bày trong docx". Đối chiếu công thức canonical trong `report/v2_draft/CH2_PROPOSED_METHODOLOGY.md` (Eq 2.1–2.5) để chép ĐÚNG. Viết md 2 phần (A. block-DCT descriptor: Eq 2.1 log-magnitude, 2.2 band=⌊rank/4⌋, 2.3 per-band mean g_{b,c}; B. gated fusion: projections Q/K/V, Eq 2.4 softmax(QKᵀ/√d_k)V, Eq 2.5 F_fused=F_s+α·context). Pandoc 2.9 chuyển LaTeX→OMML (40 khối math native Word, sửa được; delimiter |,⌊⌋,(),√ đều đúng). Render LibreOffice ra PDF kiểm tra 2 trang: mọi công thức hiển thị chuẩn. Bỏ \tag (OMML không nhận), đánh số "(Eq 2.x)" trong lead-in text.
- **File draw.io kiến trúc SFDCT lean — `report/figures/fig_2_b4dct_liveness.drawio`.** Tái tạo sơ đồ ảnh user gửi (`fig_2_b4dct_liveness.png`): face crop → (nhánh) EfficientNet-B4 spatial→F_s + block-DCT frequency→D → gated fusion (alpha init 0) → head→verdict. Mở trực tiếp app.diagrams.net. Bảng màu mặc định draw.io khớp ảnh: xám #F5F5F5/#666 (face, B4), cam #FFE6CC/#D79B00 (DCT), tím #E1D5E7/#9673A6 (fusion), xanh #DAE8FC/#6C8EBF (head). Chữ to fontSize 20, hộp vuông (rounded=0), mũi tên thẳng strokeWidth 2.5 (edgeStyle=none) kiểu lean. XML hợp lệ (5 vertex + 5 edge). Local KHÔNG có drawio CLI nên chưa render PNG kiểm tra.
- **Hình so sánh 4 phương pháp giả FF++ — `report/PL04B/gen_ff_manipulations.py` → `report/figures/fig_ff_manipulations.png`.** 2 hàng × 5 cột, cùng danh tính id 000, sau preprocess (frame đã crop mặt, resize 256). HÀNG TRÊN: 1 thật (viền xanh) + 4 phương pháp chuẩn FF++ (Deepfakes/Face2Face/FaceSwap/NeuralTextures, viền đỏ) kèm nhãn loại dấu vết. HÀNG DƯỚI: phổ block-DCT 8×8 của ảnh THẬT (viridis, tham chiếu, bỏ DC khỏi thang) + 4 footprint Real−Fake (coolwarm, thang đối xứng dùng chung ±0.0076) cho từng phương pháp. Phổ = cắt 8×8 block→DCT→log(1+|.|)→mean mọi block&frame (gộp 24 frame). Dùng frame THẬT từ `DeepfakeBench/datasets/FaceForensics++/` (real `original_sequences/youtube/c23/frames/000`, fake `manipulated_sequences/<method>/c23/frames/000_003`), KHÔNG mô phỏng. Phát hiện: FaceSwap để lại footprint mạnh nhất (đỏ rõ ở low-mid band), 3 phương pháp còn lại nhạt hơn nhiều (thang dùng chung phơi bày khác biệt độ mạnh — trung thực). Đầu tiên làm bản 1 hàng (chỉ mặt), user yêu cầu thêm hàng footprint. Sửa lỗi make_axes_locatable (matplotlib này thiếu `_axes_class`) → dùng `fig.colorbar(ax=...)`.
- **Tái tạo "Patch-based Training Augmentation Pipeline" (Image 3) bằng matplotlib — `report/PL04B/gen_augmentation_pipeline.py` → `fig_augmentation_pipeline.png`.** User gửi hình augmentation pipeline tài liệu (full-res doc + lưới patch + Selected/Rotation/Crop/ColorJitter/ToTensor/Val-Test + khối text), yêu cầu tạo lại. Script matplotlib lưới 2×4 + khối text, nhận ảnh tài liệu qua argv[1], không có thì tự sinh ảnh giả (nền giấy cũ + net chữ + mat scan tối) để vẫn chạy. Augmentation bằng PIL (rotate, crop, ImageEnhance brightness/contrast/saturation + dịch hue). Đã chạy ra hình mẫu khớp cấu trúc; user thay ảnh thật là ra y hệt. Cũng GIẢI THÍCH Image 4 (FFC-ResNet U-Net architecture): vẽ bằng draw.io/diagrams.net (bằng chứng: bảng màu pastel = đúng swatch mặc định draw.io #DAE8FC/#FFE6CC/#D5E8D4..., container có title, arrow orthogonal + dashed skip, ⊕). Đây là project KHÁC (document image binarization), không phải datn deepfake.
- **TikZ `.tex` cho preprocess pipeline — `report/figures/fig_2_preprocess_pipeline.tex`.** User gửi ảnh mẫu (raw frame → MTCNN → align → crop+pad → resize 256px → normalise → nhánh xuống YCbCr→8×8 block-DCT, kèm chú "spatial stream continues to the backbone"), yêu cầu dựng lại bằng `.tex`. Dựng khớp style họ figure đơn sắc `*_sigproc.tex` sẵn có (cùng `\documentclass[border=12pt]{standalone}` + tikz `arrows.meta,calc,positioning`, cùng `op`/`arr`, font scriptsize): chuỗi 6 box ngang nối bằng arrow Stealth, hộp block-DCT highlight (line width 0.9pt + fill black!10) đặt dưới "normalise" với mũi tên xuống, ghi chú "spatial stream continues to the backbone" màu xám bên phải. LƯU Ý: máy local KHÔNG có pdflatex/xelatex/lualatex/tectonic (các `.tex` anh em cũng chưa có PDF ⇒ user build nơi khác như Overleaf) nên chưa biên dịch kiểm tra được, nhưng cú pháp khớp đúng family đang dùng.
- **10 slide TRỌNG TÂM (slide hội đồng tập trung) — `report/PL04B/SLIDES_FOCUS_10.md`** (xóa bản 4-slide cũ, đã bao trọn). User chốt 4 chủ điểm, mỗi chủ điểm 2 slide, riêng phương pháp (chính) 4 slide: (1) Tổng quan dự án = S1 bài toán & động lực + S2 mục tiêu & phạm vi; (2) Phương pháp = S3 kiến trúc 2 nhánh + S4 đặc trưng block-DCT 48 chiều + S5 cổng hợp nhất + S6 SFDCT-HFF & tái dùng liveness; (3) Kết quả = S7 giao thức cross-dataset + S8 kết quả & đọc trung thực; (4) Kết luận = S9 kết luận & đóng góp + S10 hạn chế & hướng phát triển. Mỗi slide: bullet English + hình + chỗ chỉ tay + key message + kịch bản nói VN (xưng "em") + Q&A hội đồng + điều cần tránh. Thiết kế qua workflow (design→verify liêm chính→render); bộ kiểm bắt+sửa các số bịa (bỏ "1792 kênh" gán sai feature-map ở M1, bỏ tên "SPSL", bỏ tổng khung 159.600/16.420 không có trong CH2/CH4, bỏ AUC in-dist 0.95-0.97). LƯU Ý S5: user GHÉT "floor guarantee" làm tiêu đề (khó nói, dễ kẽ hở) → đóng khung lại là *cơ chế hợp nhất* + zero-init là thiết kế an toàn + đồng hồ đo đóng góp (số gate thật từ fig_3_13: 1792 kênh, mean +0.0000, std 0.0013, max|α| 0.0232). 2 slide bị loại khỏi nhóm trọng tâm trước đó (eKYC operating point vì dễ bị vặn "chỉ bắt 23%") nay thành slide lướt. Nguồn: INTRODUCTION.md + CONCLUSION.md + CH1-CH4. Verify hình thật trong report/figures/.
- **Bản slide rút gọn cho talk ngắn → chốt 19 slide** (`report/PL04B/SLIDES_DEFENSE_15min.md`). User: "15-16 slide nội dung + 2 diagram + 1 screen", rồi làm rõ "tổng gần 20 ĐÃ BAO GỒM 3 slide phụ trợ" ⇒ 16 nội dung + 2 diagram + 1 screen = **19 slide**. Bản đầu cô đọng quá sâu (13 nội dung) → khôi phục 3 slide đã gộp: tách "Objectives and Scope", thêm slide nền "block-DCT→48-dim descriptor", tách "Explainability" khỏi "Liveness results". 2 slide diagram = thiết kế hệ thống (`fig_2_1_usecase`/`fig_2_2_architecture`) + luồng cascade eKYC (`fig_2_4_activity_ekyc`/`fig_2_6_sequence_ekyc`); 1 slide screen (`screenshot_demo_image_detect`). ĐỐI CHIẾU lại toàn bộ với chapter MD (user yêu cầu "bám md từng chapter"): số liệu khớp CH4 từng chữ (0.7497/0.7572/0.7695, CI chứa 0, 0.9514/FPR5%/catch0.2298, confusion 5339/281/8318/2482, liveness 0.9829/6.85%/2.86%/10.83% + 0.9776); phương pháp khớp CH2 (48-dim, gate α=0 Eq2.5 floor-guarantee, HFF residual+multi-scale+residual-guided attn); nền block-DCT khớp CH2§2.3.3a + 3 dấu vết CH1. Verify 18/18 hình tồn tại trong `report/figures/`. Time budget ~15-17 phút + 5 câu hỏi hội đồng.
- **20 slide bảo vệ cô đọng từ v2** (`report/PL04B/SLIDES_20_DEFENSE.md`) — chỉ phần chính: 1 title, 2-3 bài toán/scope, 4-5 nền tảng tần số+DCT, 6-10 phương pháp (SFDCT arch/feature 48-dim/gated fusion floor-guarantee/HFF/liveness), 11-12 data+setup, 13-18 kết quả (AUC 0.7497/0.7572/0.7695, training dynamics, TRUNG THỰC: CI chứa 0/single seed/không SOTA, operating point 0.9514/FPR5%/catch 0.2298, Grad-CAM+t-SNE, liveness 0.9829/ACER6.85%), 19 system 1 slide, 20 conclusion+limit+future. Bỏ web tech/DNS/AWS/use-case/sequence/DB. Mỗi slide: title+bullet (English slide-ready) + hình (tên file thật, đã verify 19/19 tồn tại) + speaker note (VN). Kèm time budget ~18 phút + 5 câu hỏi hội đồng dự kiến (gồm câu 0.7551 vs 0.7695). Số liệu = bản đã verify.
- **Thêm mục Conclusion cuối mỗi chương (user yêu cầu)** — §1.6 (lý thuyết nền), §2.5 (phương pháp SFDCT+HFF+liveness), §3.5 (thiết kế hệ thống), §4.5 (kết quả trung thực: 0.7497/0.7572/0.7695, CI chứa 0/không SOTA/single seed, liveness 0.98/ACER 6.85%). Mỗi mục 4-5 câu, register dễ-hiểu+formal+gọn, không em-dash/`;`, không số cứng cross-ref. Forward-ref kiểu "the next chapter" (không số). Khớp cấu trúc bản gốc (mỗi chương có Conclusion).
- **Sửa mâu thuẫn 0.7551 vs 0.7695 ở đường train HFF (user bắt: hình 0.7551 nhưng báo cáo 0.7695).** VERIFY: load `report/evidence/ablation_cdfv2/pickles/hff_r3/metric_dict_best.pickle` (16,420 frame) → AUC lưu 0.769476, recompute từ pred/label = **0.7695**, video_auc 0.8269 (khớp báo cáo) ⇒ 0.7695 là best-checkpoint THẬT (DeepfakeBench test 2 lần/epoch, lưu lần tốt nhất, rơi GIỮA epoch). 0.7551 = đỉnh 11 điểm CUỐI-epoch trong `logs/hff_r3.console_capture.txt` (log per-event đầy đủ của HFF đã MẤT trên vast box). Figure cũ gọi 0.7551 là "best" → sai. SỬA `gen_fig_3_9.py`: thêm đường best-checkpoint 0.7695 (dash-dot) + chú thích "best 0.7695 caught at a mid-epoch test", đổi nhãn "epoch-end peak 0.7551"; cập nhật caption + prose Ch4 Fig 4.6 (giải thích save_best 2×/epoch). KIỂM B4/SFDCT: log đầy đủ 21 event, peak cross-dataset = 0.7497 / 0.7572 = headline ✓ (không dính lỗi, chỉ HFF do mất log). Lưu ý: đường đầy đủ 21-event của HFF KHÔNG dựng lại được (log mất) — bịa điểm trung gian là không trung thực, nên hình thể hiện điểm có thật + đường best-checkpoint verify được.

## 2026-06-16

- **Sửa khung §2.4 liveness theo CNN end-to-end (user hỏi: có trích xuất đặc trưng à?).** Tiêu đề mượn template ML cổ điển ("Feature Extraction / Data Preprocessing / Feature Selection and Filtering / Result Classification") → SAI vì liveness là CNN end-to-end (mạng tự học đặc trưng, không có khâu feature-engineering riêng). Đổi: 2.4 Liveness Detection / 2.4.1 Model Architectures / 2.4.2 Datasets and Preprocessing / 2.4.3 Role in the eKYC Cascade / 2.4.4 Training and Evaluation. Bỏ chữ "feature extraction differs…", "feature-selection decision", "feature extractor"→"backbone"; thêm câu "both learn features end to end rather than through a separate feature-engineering step". §2.3 deepfake vốn dùng "Description of Individual Modules" nên không dính. Memory cập nhật để khỏi tái phạm.
- **Ch3 §3.4 Database Design = ERD + mô tả ngắn (user: chỉ ERD, ko đặc tả cột).** ERD cũ (`fig_erd_backend.png`) thiếu nhiều bảng (chỉ 7, THIẾU liveness_checks!) → vẽ lại `fig_erd_backend_v2.png` (`report/PL04B/gen_erd.py`, matplotlib) đủ **8 bảng lõi** theo scope user chốt: tenants, users, invitations, api_keys, detections, liveness_checks, audit_logs, jobs (giữ video→jobs; BỎ webhooks/webhook_deliveries/notifications/model_versions). PK/FK + đường quan hệ (tenant là gốc multi-tenant). §3.4: mô tả ngắn + ERD, KHÔNG bảng đặc tả cột, văn không số cứng, không em-dash/`;`. Gỡ "and webhooks" khỏi mô tả actor Developer (Ch3) cho nhất quán. Memory cập nhật DB Design = DONE.
- **Bỏ số cứng cross-ref trong VĂN XUÔI Ch2/Ch3/Ch4 (user: đổi thứ tự ko phải sửa tay).** Workflow 3 agent + verify PASS. Thay "Figure 2.1/Table 2.4/Section 2.3.1/Chapter 4/Equation (2.4)" trong prose → tham chiếu mô tả ("the figure below", "the following table", "shown earlier", gọi theo khái niệm "the floor guarantee of the zero-initialised gate", "the results chapter"). Ch2 ~25 chỗ, Ch3 11, Ch4 25. GIỮ số ở caption (`*Figure 2.1: …*`) + `\tag{}` công thức (để có Danh mục hình/bảng). Hướng "trên/dưới" verify đúng hết. Grep độc lập: 0 số cứng trong prose cả 3 file. Pref đã vào memory. CÒN: Ch1 + Introduction + Conclusion chưa bỏ số (nếu muốn áp tiếp); Intro/Ch3/Ch4/Conclusion chưa qua pass register dễ-hiểu+gọn như Ch1/Ch2.
- **2 chỉnh chính xác/liêm chính (user hỏi).** (1) AUC Eq (1.5): user đưa công thức trapezoidal → xác nhận tương đương Mann-Whitney; đổi Eq (1.5) sang **trapezoidal** (định nghĩa area-under-ROC, khớp cách sklearn/DeepfakeBench tính thật) + giữ 1 câu diễn giải ranking + mốc 1.0/0.5. (2) Bảng 2.6 augmentation: user hỏi "real from deepfakebench?" → XÁC MINH khớp `training/config/detector/core.yaml` data_aug (flip 0.5, rotate ±10°, blur 3-7px, brightness/contrast ±0.1, JPEG q40-100) ⇒ THẬT; thêm tham số vào bảng cho xác minh được; BỎ "applied cautiously so spectral traces survive" (diễn giải ý đồ không có trong config) ở bảng+prose+2 echo §2.4 → factual "moderate compression range".
- **Ch1 + Ch2 theo register "dễ hiểu + formal + gọn" (user pref nhiều vòng).** Ch1: viết lại dễ hiểu → formal-hoá (bỏ we/you/signpost suồng sã) → CÔ ĐỌNG ~74% (cắt giảng sigmoid/gradient-descent/walkthrough từng số hạng; BCE còn 1 câu + ví dụ 90/55 nửa mệnh đề). User chốt mức: cô đọng nhưng ưu tiên rõ (gọn quá thì nới lại chút), áp TOÀN chương không riêng hàm loss. Ch2: viết lại dễ hiểu+formal (verify PASS liêm chính: không tái nhập FcaNet/S-lever) + đang condense cùng mức & sửa 4 lỗi (Eq 2.6 numbering, 2 `;`, câu lặp Table 2.1, lý do cascade lặp 3 lần). Quy tắc đã vào memory [[thesis-v2-restructure]].
- **SỬA OVERCLAIM liêm chính ở Ch2 (user bắt: FcaNet có thực được dùng?)** — AUDIT config thật: naive SFDCT (`efficientnetb4_sfdct.yaml`) có `dct_fca_attention:false`, `dct_use_sign:false`, `dct_srm_residual:false`, `use_dct_fomixup:false`, `use_single_center_loss:false`, `dct_drop_low_bands:0` ⇒ naive = ContentDCT (16 band×YCbCr mean, bỏ DC, 0 param freq) + zero-init gated cross-attn, KHÔNG có FcaNet/S1/S2/FoMixup/SCL/drop-low. FcaNet = lever Fix2 (=0.7523, thua naive 0.7572) + áp lên spatial trunk chứ không "re-weight frequency descriptor". ĐÃ SỬA: bỏ §2.3.3(b) FcaNet khỏi Ch2 (modules còn a=block-DCT chain, b=gated cross-attn, c=HFF), bỏ "frequency channel attention" ở 2 câu liệt kê đóng góp (Ch2 l.112/134) + mệnh đề FcaNet ở Ch1 l.140; [5] giờ cite 1 lần ở Ch1 §1.2.3 như related-work (không nhận dùng). Bảng 2.10 sửa "adopted in this thesis"→ naive giữ 16 band (bỏ DC), drop-low-band(=3) là của HFF. S1/S2/FoMixup/SCL: Ch2 không mô tả → không overclaim. Ghi memory model thật để không tái phạm.
- **Ch1 viết lại theo register DỄ HIỂU + FORMAL (user pref, làm Ch1 trước)** — 2 vòng: (1) workflow 5-mục viết lại văn cho sinh viên hiểu ngay lần đầu (gloss thuật ngữ, giải thích trực giác công thức), giữ NGUYÊN bảng 1.1–1.7/hình 1.1–1.3/eq 1.1–1.7/trích dẫn/số; (2) bản đầu lỡ casual (38 we/you/our, signpost "in plain terms/put simply/think of", ẩn dụ "lights up/busy wallpaper/shortcut wire/annoys") → workflow formal-hoá: bỏ hết ngôi 1/2, reformalise 17 cụm colloquial, GIỮ độ rõ (không quay lại hàn lâm đặc). Verify PASS cả 2 vòng: không mất nội dung, formal mà vẫn dễ đọc. Sửa caption Fig 1.3 `*`→`_` cho đồng nhất. Cán cân "accessible + formal" đã vào memory [[thesis-v2-restructure]]. CHỜ user duyệt giọng Ch1 → áp cho Intro + Ch2–Ch4.
- **Cắt forward-ref chương + giảm dấu `;` toàn 6 file v2 (user pref)** — workflow 6 agent + verify PASS. Forward-ref: mỗi chương chỉ còn 1 deferral cần thiết ("results reported in Chapter 4") — Ch1 14→1, Ch2 24→1, Intro→1, Ch3/Ch4/Conclusion 0; list "Structure of the Thesis" chừa nguyên. Dấu `;`: Intro 3→0, Ch1 18→1, Ch2 28→1, Ch3 20→3, Ch4 11→0, Conclusion 2→0 (sót lại đều trong ô bảng = ngăn list, hợp lệ); câu dài tách thành câu ngắn. Không mất số/trích dẫn/eq, không em-dash. 2 quy tắc đã vào memory [[thesis-v2-restructure]].
- **Sinh 7 hình minh hoạ v2 + gắn ảnh + finalise Ch2 (user: sinh động nhưng tiết chế)** — `report/PL04B/gen_v2_figures.py` tạo 7 hình sạch tối giản: `fig_1_convolution`, `fig_1_rest_api`, `fig_2_preprocess_pipeline`, `fig_2_dct_feature_design`, `fig_2_b4_liveness`, `fig_2_b4dct_liveness`, `fig_4_deployment` (sửa mũi tên deployment: backend→model service + backend→database, bỏ db→model sai). Kiểm hình ảnh thực tế OK. TIẾT CHẾ: KHÔNG thêm logo/AWS-icon/ROC-concept (không được tham chiếu); BỎ 2 hình filler Ch2 (data tree, label classes). Gắn ảnh: Ch1 Fig 1.1/1.3; Ch4 Fig 4.17 deployment; Ch3 đã có 3.1–3.6. Workflow finalise Ch2: dựng 6 bảng (2.1/2.4/2.5/2.6/2.9/2.10), gỡ caption trùng (Fig 2.1/2.5), đánh số hình 2.1–2.11 theo thứ tự xuất hiện + cập nhật tham chiếu; verify PASS (không mất số/eq/cite, không em-dash). Symlink `v2_draft/figures→../figures` để ảnh render khi review + khớp lúc ghép. CÒN deferred: DB design (Ch3 §3.4), 5 screenshot (cần app). Minor: Ch3 Fig 3.2 (architecture) xuất hiện trước Fig 3.1 (use-case).
- **Phá "tường chữ" Ch1 (user: dùng hình/bảng/chia nhỏ, mức vừa phải)** — §1.1.1: 3 đoạn họ giả mạo → Bảng 1.1, đoạn 3 dấu vết → Bảng 1.2; §1.2.1: thêm Hình 1.1 convolution (placeholder CREATE `fig_1_convolution.png`), biến thể conv → Bảng 1.5, chia nhỏ các đoạn stride/pooling/feature-hierarchy/residual (giữ Eq x+F(x), [29][33]). ĐÁNH SỐ LẠI Ch1: Bảng 1.1–1.7 (1.1 families, 1.2 traces, 1.3 FF++ methods, 1.4 datasets, 1.5 conv-variants, 1.6 scaling, 1.7 DCT-regions), Hình 1.1–1.3 (1.1 convolution, 1.2 MBConv, 1.3 REST) — mọi tham chiếu in-text đã cập nhật. Verify body PASS (không mất nội dung, không em-dash, hết wall-of-text, không lặp prose). Xoá khối HTML "ASSEMBLY NOTES" lỗi thời ở cuối (số cũ + em-dash). CÒN tạo asset `fig_1_convolution.png`.
- **Quét bullet 6 file v2 (user pref: liệt kê thuần → list `-`)** — workflow 6 agent (1/file, tiêu chí bảo thủ) + verify chống bullet-hoá quá đà. Chuyển: Ch1 4 chỗ (3 lý do B4 §1.2.2, 3 lý do DCT §1.4.1, print/replay/mask §1.4.2, 3 đặc tính Next.js §1.5.2); Conclusion 1 (Development Directions → 5 bullet). Intro/Ch2/Ch3/Ch4 giữ nguyên (lý lẽ/kết quả là văn xuôi; liệt kê đã ở bảng/đã là list). Verify PASS: không mất số/trích dẫn/công thức, không em-dash rendered. Cũng đổi "Structure of the Thesis" (Intro) từ đoạn văn → 5 bullet. Quy tắc đã vào memory [[thesis-v2-restructure]].
- **Viết Ch3 + Ch4 + Conclusion v2 — verdict PASS** (`report/v2_draft/CH3_SYSTEM_ANALYSIS_DESIGN.md`, `CH4_IMPLEMENTATION_RESULTS.md`, `CONCLUSION.md`). Ch3: 3.1 use case (Fig 3.1 + kiến trúc Fig 3.2) → 3.2 đặc tả UC (UC-01/02/03 + supporting + API, Bảng 3.1–3.8 chép verbatim FULL) → 3.3 sequence (Fig 3.3–3.6 activity+sequence) → 3.4 DB Design = DEFERRED (heading + TODO). Ch4: 4.1 env/hyperparam (Bảng 4.1–4.3) → 4.2.1 deepfake (Fig 4.1–4.13, Bảng 4.4 cross-dataset, 4.5 calibration) → 4.2.2 liveness (Fig 4.14–4.16, Bảng 4.6) → 4.3 deploy (Fig 4.17 CREATE, Bảng 4.7) → 4.4 app UI (Fig 4.18 có sẵn, 4.19–4.23 NEEDS-APP, Bảng 4.8). Conclusion: Achieved/Limitations/Future. VERIFY đối kháng: **mọi số Ch4 khớp FULL.md** (B4 0.7497/SFDCT 0.7572/HFF 0.7695; video 0.8203/0.8083/0.8269 CI chứa 0; ngưỡng 0.9514 catch 0.2298 FPR 0.0500; confusion 5339/281/8318/2482; liveness B4 0.9829 ACER 6.85% APCER 2.86% BPCER 10.83%, B4+DCT 0.9776 ACER 7.54%); caveat đủ; không em-dash rendered; không lead-in chương; không lặp bảng; 31 refs. Stitcher fix va chạm số bảng (liveness 4.4→4.6…). CÒN: front matter, materialise bảng/hình CREATE, screenshot (cần app), DB design.
- **Áp 2 quy tắc trình bày (user) cho Intro/Ch1/Ch2 v2 + ghi memory cho phần sau.** (1) Bỏ đoạn mở đầu CẤP CHƯƠNG → tiêu đề chương đi thẳng xuống §1.1/§2.1; (2) KHÔNG dùng em-dash `—`/en-dash `–` trong phần hiển thị (prose/caption/ô bảng) → thay bằng phẩy/ngoặc/hai chấm/tách câu; tiêu đề chương dùng dấu hai chấm (`CHAPTER N: TITLE`); bullet giữ `- ` đầu dòng. Workflow 3 agent gỡ dash tại chỗ: Intro 4, Ch1 6 (gồm title), Ch2 24. XÁC MINH bằng python (tước HTML comment đa dòng rồi đếm): rendered em=0/en=0 cả 3 file. Dash còn lại CHỈ trong HTML comment CREATE (sẽ thành bảng/hình thật sau, vẫn né em-dash). Cũng sửa Introduction: "Structure of the Thesis" + Step 5 đổi 3 chương cũ→4 chương (results→Ch4), bỏ bold lẻ. Quy tắc đã vào memory [[thesis-v2-restructure]] → Ch3/Ch4 tự tuân.
- **Viết Introduction + Chương 2 (Proposed Methodology) v2** — `report/v2_draft/INTRODUCTION.md` + `CH2_PROPOSED_METHODOLOGY.md`. Workflow 4 writer (intro / §2.1-2.2 / §2.3 / §2.4) + stitch 2 file + verify đối kháng (effort high). Ch2 gom ĐÓNG GÓP dời từ Ch1: §2.3.3 block-DCT chain (log-magnitude Eq 2.1 `D=log(1+|F|)`, band=⌊rank/4⌋ Eq 2.2, 48-dim 16band×YCbCr Eq 2.3), cross-attention spatial-Q/freq-KV Eq 2.4, **gate α Eq 2.5 `F_fused=F_s+α·context` (α init 0)**, SE-tần-số kiểu FcaNet, biến thể SFDCT-HFF; §2.4 liveness B4 vs B4+DCT (metric APCER/BPCER/ACER định nghĩa ở Ch1). Eq (2.1)-(2.7). Liêm chính: mọi số truy về FULL.md, KHÔNG rò AUC vào Ch2 (forward→Ch4). Intro có Input/Output/Main Tasks + Table 0.1/0.2. SỬA sau verify: mục "Structure of the Thesis" + Step 5 còn mô tả 3 chương cũ (results ở Ch3) → đổi sang 4 chương (results→Ch4); bỏ bold lẻ. Cảnh báo "Eq (1.7) sai" của verifier là FALSE POSITIVE (nó đối chiếu §1.7 Attention của FULL.md, còn Ch1 v2 thì Eq 1.7 = 2D DCT → đúng). CÒN CREATE: nhiều bảng/hình minh hoạ Ch2 (pipeline I/O, label map, augmentation, 48-dim design, drop-low-band, b4/b4dct liveness arch, data tree, preprocess). CHỜ duyệt rồi viết Ch3 (use case/sequence, DB để sau) + Ch4.
- **VIẾT LẠI Chương 1 v2 (lần 2) — PASS toàn bộ** (`report/v2_draft/CH1_THEORETICAL_BACKGROUND.md`, ghi đè bản thử). Sửa đúng 5 điểm user (giám khảo) nêu: (1) gõ hẳn 7 công thức đánh số (1.1)–(1.7): decision rule, compound scaling, scaled dot-product attention, BCE, AUC Mann–Whitney, DCT 1D/2D + ACER untagged; (2) cross-ref sửa hết "Chapter 3"→"Chapter 4" (kết quả), method→"Chapter 2"; (3) TÁCH lý thuyết khỏi đóng góp — dời chuỗi block-DCT (16 band/log-mag/YCbCr/drop-low) + gate x+α·context + SE-tần-số-FcaNet + cross-attn spatial-query/freq-KV + hình zigzag chi tiết SANG Chương 2 (Ch1 chỉ giữ DCT/attention/SE tổng quát); (4) rải lại trích dẫn từ danh mục FULL.md [1][2][3][4][10][13][21][23][24][26][27][29][30][31] + 3 nguồn MỚI cần thêm vào master list [32] MobileNetV2 [33] ResNet [34] JPEG; (5) định nghĩa hẳn APCER/BPCER/ACER (cite ISO/IEC 30107-3 [13]) + nền PAD print/replay/mask ở §1.4.2, đổi tên §1.4 bỏ "Downstream"→"Frequency-Domain Analysis and Liveness". Verifier đối kháng: mọi số truy về FULL.md, không bịa. Workflow 5 writer + stitch + verify (effort high). CÒN: Figure 1.2 REST API chưa có asset (TODO). User tự polish thêm prose trong file. CHỜ duyệt để viết Intro+Ch2+Ch3+Ch4+Conclusion+front matter.
- **Viết thử Chương 1 cho bản tái cấu trúc (v2)** — `report/v2_draft/CH1_THEORETICAL_BACKGROUND.md`. Workflow (ultracode): 5 agent viết song song 5 mục (1.1 Problem Domain→1.5 Web Tech) đọc đúng đoạn nguồn FULL.md theo blueprint → 1 agent ghép+đồng nhất giọng+đánh số hình/bảng theo thứ tự xuất hiện → 1 agent kiểm tra liêm chính đối kháng + lint anti-AI. Ch1 đảo thứ tự (AI/domain trước, web-tech cuối), liveness = mục phụ §1.4.2. Hình: 1.1 MBConv, 1.2 zigzag, 1.3 REST API; Bảng 1.1–1.5 đúng thứ tự. Verifier bắt 1 lỗi BỊA "(c23)" (FULL.md chỉ ghi "moderate compression level") → đã bỏ. Tôi sửa thêm: link ảnh Fig 1.2 trỏ NHẦM `fig_3_11_frequency.png` (biểu đồ kết quả) → đúng `fig_1_3_zigzag_detailed.png`; Fig 1.1 MBConv nối `fig_1_2_mbconv.png` (asset có sẵn, bỏ TODO); căn tên §1.1/§1.4 khớp template. Còn 8 TODO (công thức omitted + REST asset + mục mỏng). CHƯA viết các chương khác — chờ user duyệt văn phong/độ sâu.
- **Blueprint tái cấu trúc 4 chương** — `report/RESTRUCTURE_V2_PLAN.md`. Workflow 6 agent map FULL.md→template (Intro→Ch1 Theory→Ch2 Method→Ch3 System Design→Ch4 Impl&Results), gồm: Understanding/Assumptions/Decision Log, outline đích, bảng mapping từng chương, bản đồ đánh số lại 23 hình + 26 bảng, danh sách 14 gap/[TODO], danh sách số liệu phải giữ verbatim, open-risks. Quyết định khoá: tách 3→4 chương; Ch1 reorder; liveness=downstream; DB Design=placeholder [TODO]; CHỈ nguồn FULL.md, không bịa số. Brainstorming: Understanding Lock đã xác nhận; user chọn "viết lại từ đầu theo template" + "DB Design [TODO]" + "chỉ FULL.md" → viết thử Ch1 trước.
- **PDF luận văn chính thức → Markdown full** — `report/102220041_LeNgocThanh_Thesis_FULL.md` (+ `.raw` backup). pymupdf4llm chuyển 95 trang (text thật, không scan): 142.9k ký tự, 173 heading, 367 dòng bảng, 41 marker hình omitted. Dọn nhiễu chân trang KHÔNG đụng nội dung: bỏ 88 dòng tên đề tài in nghiêng + 83 dòng "Performed Student: Le Ngoc Thanh" + 72 số trang đứng một mình; gộp dòng trống thừa. Hạn chế: mọi heading phẳng `##` (PDF không mã hoá cấp), bảng mục lục dấu chấm dẫn render xấu, hình không nhúng (chỉ marker vị trí).

## 2026-06-15

- **Gated cross-attention fusion vẽ lại bằng LaTeX/TikZ** (`report/figures/fig_arch_gatefusion_sigproc.tex`) — style sig-proc đơn sắc KHỚP mẫu `fig_arch_b4_sigproc.tex` (cùng preamble, macro `\stk`/`\vbar`, style `op`/`arr`). Nội dung khớp PNG gate-fusion: nhánh spatial B4→F_s (R^1792×8×8) + nhánh Block-DCT 8×8 16 bands→D (K,V); cross-attention Q=F_s K,V=D → context c → gate ×α (init 0) → ⊕ với đường floor/identity F_s; ra F_s'=F_s+α·c; note "α=0 lúc init ⇒ F_s'≡F_s (floor≥B4), α chỉ mở khi giảm loss". Máy KHÔNG có pdflatex → chưa compile/preview tại chỗ; đã soát syntax tay (fix chồng nhãn ở stack F_s'). User tự `pdflatex` để render.
- **Hình chi tiết zigzag→16 dải→vector 48** (`report/PL04B/gen_fig_1_3_zigzag_detailed.py` → `report/figures/fig_1_3_zigzag_detailed.png`): 3 bước có mũi tên — ① block 8×8 zigzag path + số 0–63 + tô màu theo dải + chấm DC; ② 16 hàng "dải 0–15" mỗi hàng 4 hệ số (gom 4 zigzag liên tiếp = 1 dải); ③ thanh 16 dải → trung bình |DCT|/dải × 3 kênh YCbCr = vector 48 (global48), kèm note bỏ DC/drop_low_bands. Thứ tự zigzag + band=rank//4 LẤY ĐÚNG từ sfdct_core.py::zigzag_band_of (không bịa). Nhãn TIẾNG ANH (user yêu cầu) cho THESIS_REPORT_EN; KHÔNG ghi đè bản gốc fig_1_3 — file _detailed riêng.
- **glm_deepfake dev → DEPLOY (deepguard.ddns.net) predict + trang upload-predict giống playground**. (A) Xác minh deploy: nginx route `/api/*`→backend:8000, nên API base deploy = `https://deepguard.ddns.net/api` (HTTPS cert hợp lệ); `/api/health`→200, `/api/v1/detect/image` no-key→403 (route OK). Login deploy demo dev@vietbank.vn/Password123! OK (DB deploy có seed) → tạo key `sk-dg-wDz5...` trên deploy → predict trực tiếp `verdict REAL 0.0516`. (B) Trỏ `glm_deepfake/.env` sang deploy + key đó (giữ local fallback comment; .env đã gitignore `.env*` nên key không leak); restart glm :3010 → POST /api/deepfake → `is_real:true synthetic_score:0.0516 mode:"live"` = khớp deploy (local 0.0505) ⇒ glm dev gọi đúng model deploy. (C) Trang mới `glm_deepfake/src/app/deepfake/page.tsx` (client, shadcn Card/Button/Badge): upload ảnh→base64→POST /api/deepfake→hiện REAL/FAKE + %fake + checks + badge MODEL THẬT/DEMO; render HTTP 200. `.env.example` ghi rõ 2 base URL (local :8000 vs deploy /api) + key local≠deploy. Đổi lại local: bỏ comment 2 dòng local trong glm/.env.
- **Demo glm_deepfake predict qua API key — CHẠY THÔNG**. glm_deepfake (`.env` đã có `DEEPGUARD_API_KEY=sk-dg-gy0q...`, hợp lệ trong deepguard-db hiện tại) gọi `/api/deepfake`→`analyzeDeepfakeImage`→DeepGuard `/v1/detect/image` (Bearer key)→`:8501` model thật. Khởi động glm_deepfake bằng `node_modules/.bin/next dev -p 3010` (port script mặc định 3000 TRÙNG datn frontend → đổi 3010). Test: POST base64 mặt thật → `{is_real:true, synthetic_score:0.0505, mode:"live", details:"naive-sfdct-cdfv2-0.7572 verdict=REAL"}`. `mode:"live"` = dùng model thật (key OK); nếu key rỗng/sai → `mode:"demo"`. Lấy key mới: login dev@vietbank.vn (dashboard :3000) → API Keys → Create → copy `sk-dg-...` → dán glm_deepfake/.env → restart. Services đang chạy: :8501 :8502 :8000 deepguard-db :3000(datn FE) :3010(glm).
- **Slider chỉnh ngưỡng liveness trên FE** (`frontend/src/components/deepguard/liveness-page.tsx` + `lib/api.ts`): `detectLivenessPassive(file, apiKey, threshold?)` nay nối `?threshold=` vào URL; trang liveness thêm state `threshold` (default 0.125) + input range 0–1 step 0.005 (chỉ hiện chế độ passive), hiển thị %/quy tắc "P(live) < x% ⇒ SPOOF". Kéo slider → override ngưỡng per-request, không cần sửa .env/restart backend. `tsc --noEmit` exit 0. Active mode CHƯA nối threshold (endpoint /active chưa nhận param) — thêm sau nếu cần.
- **Ngưỡng liveness thành setting tùy chỉnh + hạ default 0.8743 → 0.125** (mặt thật webcam OOD chỉ ~0.5 P(live) bị gắn nhầm SPOOF vì ngưỡng dev-EER 0.8743 quá chặt). Sửa: `config.py` LIVENESS_THRESHOLD default 0.125 + thêm LIVENESS_MARGIN (default 0.0 = cắt nhị phân sạch); `.env` LIVENESS_THRESHOLD=0.125 + LIVENESS_MARGIN=0.0; `services/liveness.py` đọc ngưỡng/margin TỪ config (trước hardcode 0.5/0.08) + `_verdict_from_score(score,threshold,margin)` nhận override + guard chia 0; `_real_liveness`/`run_liveness_check` nhận `threshold=`; router `/v1/detect/liveness` thêm query `?threshold=` (0-1) để override PER-REQUEST không cần restart. P(live)<threshold ⇒ SPOOF. TEST qua backend: mặt thật 0.6867 → default 0.125=LIVE (trước SPOOF), override 0.9=SPOOF; mặt 0.0029 → SPOOF. threshold_used trả về đúng. LƯU Ý liêm chính: 0.125 là calib DEMO (nới rộng, spoof 0.125-0.5 sẽ lọt); BÁO CÁO vẫn dùng dev-EER 0.8743 (EER 6.99%). Frontend không cần sửa — .env 0.125 áp cho mọi call; muốn slider UI thì thêm sau.
- **Thêm crop mặt cho nhánh liveness + test local** (sửa lệch train/serve). PHÁT HIỆN: LCC-FASD là mặt ĐÃ crop vuông (mặt đầy khung, 320–666px), nhưng `serving/liveness_server.py` + `backend/_real_liveness` gửi NGUYÊN frame webcam (mặt nhỏ+nền) → chỉ resize → model bắt nhầm mặt thật thành SPOOF. FIX: thêm `serving/face_crop.py` (detector offline dlib→Haar→fallback nguyên ảnh, crop VUÔNG + lề 1.3x khớp `extract_face_MTCNN`), gắn vào `liveness_server.py` (env `LIVENESS_FACE_CROP=1` mặc định bật, `LIVENESS_CROP_EXPAND=1.3`, trả thêm `face_cropped`). Backend KHÔNG cần sửa (đã forward bytes sang :8502, `LIVENESS_INFER_URL` đã set). Bản deploy = root `serving/` (redeploy.sh chạy `uvicorn serving.liveness_server:app`); `DeepfakeBench/serving/` là copy cũ KHÔNG dùng. dlib KHÔNG có trong serving/requirements → EC2 tự fallback Haar (opencv-headless đã có). TEST LOCAL (`serving/test_liveness_crop.py` + chạy thật :8502 cuda): (1) eval đã-crop: real P(live) 0.780 / spoof 0.016, face_found 40/40 — crop không phá số; (2) mô phỏng frame webcam: lệch |ΔP(live)| no-crop 0.407 → crop 0.114; Haar-only (đường EC2) 0.331→0.095. (3) **E2E qua HTTP service** (đúng chỗ backend gọi): cùng 1 frame webcam mặt thật → crop TẮT 0.0164 (SPOOF, từ chối nhầm) vs crop BẬT 0.9926 (LIVE, đúng). (4) **FULL BACKEND E2E PASSED**: user bật Docker → `backend/start.sh` dựng deepguard-db + uvicorn :8000; login dev@vietbank.vn/Password123! → tạo API key → POST /v1/detect/liveness bằng frame webcam → qua backend→:8502(crop)→ verdict **LIVE score 0.9926 conf 94.11** (đúng). Mạch frontend→backend→liveness đã thông phía server. CÒN: bấm tay trên UI :3000 (extension Chrome chưa kết nối nên chưa tự lái được). Services đang chạy nền: :8502 crop ON, :8000 backend, deepguard-db (docker), :3000 frontend.
- Kịch bản thuyết trình bảo vệ (`report/PL04B/KICH_BAN_THUYET_TRINH.md`): 25 slide / 20 phút, dựa ĐÚNG hình trong bản final docx (`Done PL04_B_RuotThuyetMinh final.docx` — 31 ảnh / 33 caption, đánh số đã sạch). Trọng tâm lý thuyết→kiến trúc→đánh giá; nén phần phụ (web tech, DNS/AWS, use-case/activity/sequence, app screen, deploy). Số THẬT: B4 0.7497 / SFDCT 0.7572 / HFF 0.7695 frame-level; video-level CI chứa 0 (chưa significant) — giữ narrative trung thực. Kèm bảng phân bổ thời gian + 5 câu hỏi hội đồng dự kiến & cách trả lời.
- Figure 3.4 v2 (`gen_fig_3_4_v2.py` → `fig_3_4_v2_train_dynamics.png`): 3 hàng (B4·SFDCT·HFF-R3), trái train loss, phải AUC test có CẢ FF++ in-distribution LẪN Celeb-DF cross-dataset + sàn B4 0.7497. Số THẬT parse từ `b4/sfdct.training.log` (21 test event/model). Phát hiện kể chuyện: FF++ test cao & phẳng (B4 0.9754, SFDCT 0.9769), chỉ Celeb dao động (0.66–0.757) → bác "train vô nghĩa", đó là generalisation gap. HFF-R3 chỉ có 11 Celeb cuối-epoch (log mất) → ghi rõ "FF++ per-epoch n/a", KHÔNG bịa. Trả lời câu hội đồng "sao test dao động mạnh". CHỐT: bỏ hàng HFF (mất log, không bịa) → hình 2 hàng B4+SFDCT cho gọn; wire TẠM vào MD ngay trước 3.1.5 (marker 【ẢNH-3.4v2】 + cụm '....'), chưa thay 3.5/3.7/3.9. (Từ chối yêu cầu bịa đường FF++ cho HFF — ngụy tạo kết quả.)
- Tích hợp THÊM AWS (Phase 3), đúng pattern no-op khi env trống: **(1) S3 lưu media gốc** — `storage.upload_media()` lưu ảnh/video input lên `s3://<bucket>/tenants/<tenant>/detections/<request_id>_input.<ext>` (audit eKYC; hiện chỉ lưu heatmap); **(2) CloudWatch custom metrics** — module mới `app/services/metrics.py::emit_detection()` bắn `put_metric_data` (Detections theo Source+Verdict, ProcessingLatency, ProbFake, InferenceErrors). Wire vào 4 handler: playground/api × ảnh/video. Config thêm `CW_METRIC_NAMESPACE`/`CW_REGION` (config.py + .env.example). boto3 import muộn, không raise → tắt env thì app chạy y như cũ. py_compile OK. CHƯA có endpoint presign media (mới ghi, đọc bằng key suy ra từ request_id). CẦN redeploy backend + set env S3/CW.
- Fix lỗi video deploy `Video inference failed: No module named 'torch'` (`/api/playground/detect/video` → 400): `backend/app/services/ml_video.py::_sfdct_inference_video` đang gọi `_get_detector()` (MTCNN) crop mặt TRONG backend, mà `_get_detector` `import torch` → backend (không cài torch) crash. Sửa: gửi NGUYÊN frame sang microservice `:8501` `/predict` để service tự crop (giống path ảnh `_sfdct_inference`) → bỏ `_get_detector()`/`_crop_face` khỏi path video, backend hết cần torch/MTCNN. `_real_inference_video` (torch) giữ nguyên, chỉ dùng khi `SFDCT_INFER_URL` rỗng. CẦN redeploy backend (`:8501` phải đang chạy). Lỗi `onboarding.js`/`content-script.js`/`WidgetId` là của extension trình duyệt, không liên quan.

## 2026-06-14

- Cài 29 skill `mattpocock/skills` (tdd, diagnose, triage, to-prd, write-a-skill, caveman, handoff…) bằng `npx skills@latest add mattpocock/skills -s '*' -y`. Global thất bại (PromptScript không hỗ trợ) → cài PROJECT-LEVEL vào `.agents/skills/` + symlink sang `.claude/skills/`. Cả 2 thư mục vốn untracked nên KHÔNG đụng file đã commit. Gỡ: `npx skills@latest remove --all`
- Kiến trúc model vẽ theo MẪU học thuật (Fig 2.17 Time-Distributed FCN: khối tensor 3D xếp chồng + hatching + brace chiều + hộp module xám + ký hiệu ℝ): generator `report/PL04B/gen_fig_architecture.py`, shape THẬT đọc từ `sfdct_core.py`/`efficientnetb4_sfdct_detector.py`/`sfdct_hff_core.py`. 3 hình tiếng Anh, KHÔNG số 'Figure': `fig_arch_sfdct.png` (B4 1792×8×8 + ContentDCT global48 16×3 → gated cross-attn MHA d=128/4head, α init 0) · `fig_arch_sfdct_hff.png` (B4 + block-DCT high-pass→HF-Stream 5×conv→RSAttention→HFF gate α0, R3) · `fig_arch_contentdct.png` (face→YCbCr→8×8 block→log|DCT|→zigzag 16 band→tokens 16×3)
- "Distribution Analysis in Training and Test Sets" (theo mẫu HarmonySeeker `interactive_chord_distribution.html`): generator `report/PL04B/gen_distribution_interactive.py` dựng Plotly lưới 2×3 (pie Train · pie Test · grouped-bar Real/Fake; bar FF++ 4 method · bar Celeb test · radar dataset-profile) bằng SỐ THẬT — FF++ train video 719 real/2877 fake (20/80), Celeb-DF-v2 test 5620 real/10800 fake (34.2/65.8). Xuất `report/figures/interactive_distribution_deepguard.html` (CDN, tương tác) + `fig_3_1_2_distribution_analysis.png` (kaleido, không số 'Figure')
- PL04B Ch2 UML (matplotlib, thay mermaid, theo mẫu Thành): 2.1 use-case (fix đường KHÔNG cắt ellipse — bố cục 2 nhóm trên detection/dưới quản lý, include-extend nằm hẳn phải) · 2.3 + 2.4 activity swimlane (2.4 eKYC cascade, alt 3 nhánh liveness + 3 nhánh deepfake) · 2.5/2.6 sequence (lifeline + self-message + return đứt nét + alt fragment); generator `gen_fig_usecase/activity/activity24/sequence.py`; chờ wire vào MD thay 5 block mermaid

## 2026-06-13

- PL04B fig 3.20/3.23 liveness ROC (`gen_fig_liveness_roc.py`, chạy nền): infer 2 model trên 7580 ảnh LCC-FASD eval THẬT → AUC 0.9829 (B4) / 0.9777 (B4+DCT) **khớp metrics tuyệt đối**, ROC + score-dist (live/spoof tách rõ, EER~7%); 3.19/3.22 BỎ (không có log per-epoch); wire vào MD (CẦN-BẠN 9→7). + 11 mermaid trích ra `mermaid_view.html`/`mermaid_11.md` (không render PNG được: npm chặn/extension off/kroki bị chặn vì gửi nội bộ ra ngoài)
- PL04B fig 3.17 liveness explainer (style mt16, `gen_fig_liveness.py`): ví dụ live/spoof LCC-FASD THẬT (4+4) + 16-band live vs spoof (≈giống, spoof-live cao-tần −0.0012) → giải thích trung thực §3.1.9: spoof khác về KHÔNG GIAN nhưng ≈ live ở band-DCT → nhánh tần số không giúp (B4 0.9829 ≥ B4+DCT 0.9776); wire vào MD (CẦN-BẠN 10→9, ẢNH 20→21)
- PL04B HFF t-SNE+Grad-CAM: CHẠY model HFF-R3 thật (ckpt 17-30-34, verify AUC subsample 0.7692 ≈ 0.7695 → đúng model), trích feat 1000 frame → `fig_3_10_2_tsne_hff` (tách real/fake rõ hơn SFDCT chút, khớp AUC cao hơn) + Grad-CAM ảnh fake p=1.00 `fig_3_12_2_gradcam_hff` (tái dùng infer.gradcam); gate-α-HFF bỏ (RSAttention injection → vẽ gate misleading)
- PL04B HFF companions: confusion `fig_3_12_2_confusion_hff` (4 ngưỡng, FPR5% caught **2857** vs SFDCT 2482 — HFF bắt nhiều hơn ở eKYC) + predictions `fig_3_15_2_predictions_hff` từ pickle hff_r3 THẬT (0.7695); t-SNE/Grad-CAM HFF chưa làm (viz_out không cache feat/gradcam cho HFF → phải chạy model); gate-α-HFF KHÔNG làm (gate.alpha≈0 nhưng HFF bơm freq qua RSAttention → vẽ kiểu SFDCT sẽ misleading) — hình mới chưa wire
- PL04B fig 3.13.2 (`gen_fig_gate_init.py`): hình KHÁI NIỆM đối chứng gate init 0 (SFDCT → floor B4 0.7497) vs init 0.5 (SFCL-HCMF → no floor), schematic tại t=0; zero-init tham chiếu số thật Fig 3.16, 0.5-init ghi RÕ là minh hoạ (không có model 0.5-init được train → không bịa số đo); `fig_3_13_2_gate_init.png` (hình MỚI, chưa wire vào MD)
- PL04B fig 3.16/3.15 (`gen_fig_gate_pred.py`): 3.16 phân bố gate α SFDCT từ checkpoint thật (fusion.alpha 1792 kênh, std 0.0013, max|α| 0.023 → gate gần như KHÔNG mở, floor giữ ≈ B4); 3.15 8 ví dụ test thật (2 caught + 2 accepted + 2 FP + 2 FN, verdict@0.5) — wire vào MD (CẦN-BẠN 11→10, ẢNH 19→20); phân tích t-SNE: silhouette real/fake = 0.13 (chồng lấn nặng, khớp AUC 0.76 = bằng chứng generalization gap)
- PL04B fig 3.11/3.12/3.13/3.14 (`gen_fig_pr_cm_tsne.py`): PR 3 model + AP; **confusion SFDCT 4 ngưỡng GỘP 1 hình** (default 0.5 + EER 0.406 + FPR10% + FPR5%, FPR5% khớp §3.1.6 tuyệt đối 5339/281/8318/2482, thấy trade-off caught 6790→2482); t-SNE từ feat THẬT (1792-d npz, 1500+1500, overlap khớp AUC 0.76); Grad-CAM copy bản render thật viz_out; KHÔNG số "Figure"; ghi đè fig_3_8_pr/fig_3_9_confusion/fig_3_10_tsne/fig_3_12_gradcam (MD tự cập nhật)
- PL04B fig 3.5/3.7/3.9/3.10: dựng từ log+pickle THẬT (`gen_fig_curves_roc.py`) — train-curve B4 (best test-AUC 0.7497) + SFDCT (0.7572) [loss/iter + AUC/epoch 21 test-event], HFF-R3 AUC/epoch (11đ), ROC 3 model + vạch FPR=5% (AUC khớp Bảng 3.5 tuyệt đối: 0.7497/0.7572/0.7695); KHÔNG số "Figure" trong ảnh; ghi đè fig_3_3_train_b4/fig_3_4_train_naive/fig_3_9_train_hff_r3/fig_3_7_roc (MD tự cập nhật)
- PL04B: thêm hình phổ block-DCT theo TỪNG KÊNH Y/Cb/Cr (`gen_fig_ycbcr.py`, YCbCr BT.601 khớp model) FF++ real vs fake + hiệu, gộp 200 ảnh/nhóm — minh hoạ "48 = 3 kênh × 16 band"; chênh real-fake dồn ở DC, rõ hơn ở chroma; `report/figures/fig_ycbcr_dct_spectrum.png` (hình MỚI, chưa wire vào MD)
- PL04B fig 3.4/3.6/3.8: load 3 model THẬT qua DETECTOR registry (backbone pretrained local, `gen_fig_3_4_6_8.py`) → **torchinfo CHI TIẾT** (output shape + params + % + mult-adds, depth 3, input_data bọc tuple để khớp forward(data_dict)); B4 17.55M · SFDCT 18.08M (nhánh DCT **0 param**) · SFDCT-HFF 22.77M; **bỏ số "Figure 3.x" khỏi tiêu đề mọi ảnh tự tạo** (số để caption lo) gồm cả fig 3.3; wire 3 ảnh vào MD (CẦN-BẠN 14→11, ẢNH 16→19)
- PL04B fig 3.3: generate `gen_fig_3_3.py` — năng lượng 16 band zigzag real vs fake + cột hiệu, cho CẢ FF++ (train, 4 method) lẫn Celeb-DF (test), gộp 300 ảnh/nhóm, block 8×8→zigzag khớp model; cả 2 dataset real≥fake (over-smoothing nhất quán cross-dataset); ghi đè `report/figures/fig_3_11_frequency.png` (Figure 3.3 trong MD trỏ tới)
- PL04B fig 3.1+3.2: generate lại từ số/ảnh THẬT theo logic mt12/mt07 (`gen_fig_3_1_3_2.py`) — Fig 3.1 phân bố FF++ train/test (json) + CDFv2 real/fake 5.620/10.800 (pickle SFDCT, khớp Bảng 3.4); Fig 3.2: SỬA phương pháp theo góp ý Thành — DCT-toàn-ảnh (sai) → block 8×8 DCT → 16-band → **chốt phân vai: Figure 3.2/3.2.2 = PHỔ DCT block 8×8** (real/fake spectrum + panel Real−Fake, DC loại khỏi thang màu), còn biểu đồ 16-band chuyển sang Figure 3.3; thêm bản Celeb `fig_3_2_2_celeb_realfake.png`. KIỂM LIÊM CHÍNH yêu cầu "fake đỏ góc cao-tần": đã thử global/block/FFT, cặp đơn + gộp 400 ảnh → Δ(fake−real) CHỈ dương ở DC, ÂM ở 63 hệ số còn lại (cả cao-tần); fake KHÔNG trội cao-tần (c23 xóa artifact, khớp §3.1.1 over-smoothing) → KHÔNG tô đỏ giả; ghi đè `fig_3_1_distribution.png` + `fig_3_2_preprocess_realfake.png`
- PL04B (tiếp, "do for me"): vẽ 5 sơ đồ mermaid MỚI từ mô tả thân bài (Figure 1.1 REST API, 2.7 SFDCT, 2.8 SFDCT-HFF, 2.9 B4-liveness, 2.10 B4+DCT-liveness); generate `report/figures/fig_3_9_train_hff_r3.png` từ 11 điểm HFF-R3 thật (`gen_fig_3_9.py`) rồi nối vào Figure 3.9; chốt BỎ 4 hình ROC/curve liveness (json chỉ có số cuối, không có score-array → Bảng 3.10 đã đủ); marker 20→14 CẦN-BẠN, tổng 63 (16 SỬA/16 ẢNH/11 SƠ-ĐỒ/14 CẦN-BẠN/6 DRAFT)
- Convert DOCX `Done PL04_B_RuotThuyetMinh` (luận văn EN đổ khuôn khoa, 22.9k từ) → MD giữ format ở `report/PL04B/`; phát hiện ảnh trong DOCX mất sạch (placeholder 1×1) → nối 15 ảnh thật từ `report/figures` (bỏ 3 `fig_detail_*` do Thành xác nhận SAI), bọc 6 sơ đồ mermaid render được, đánh lại số hình Ch2 trùng (2.2–2.5→2.7–2.10), sửa SUMMARY tả sai liveness (MediaPipe/ArcFace → đúng B4-liveness), draft 2 mục lý thuyết rỗng 1.11 (đổi tên khỏi trùng 1.10) + 1.12, xóa template VN sót cuối file; gắn 63 marker `【…】` (SỬA/ẢNH/SƠ-ĐỒ/CẦN-BẠN/DRAFT) + bảng tra để chép tay sang Google Docs; script tái chạy `report/PL04B/apply_edits.py`, bản gốc sạch giữ ở `*.src.md`

## 2026-06-12

- Viết lại INTRODUCTION theo y khuôn mẫu Trí (5 mục không số, 7 Step, văn ~5.5, 0 bold thân bài, 1.147 từ); bỏ 5 mục con intro khỏi TOC frontmatter; rebuild md+docx
- Viết lại TOÀN BỘ thesis EN v2 (6 file, 43.1k→38.9k từ) theo IELTS 6.0 + rule chống văn AI + rút số liệu khỏi chỗ không cần; verify PASS (0 số bịa, heading nguyên vẹn, 0 takeaway); rebuild THESIS_REPORT_EN.md + .docx (17 hình); backup bản cũ ở report/_en_v2.bak-20260612/
- Giải đáp phân cấp heading TOC theo mẫu Trí: front-matter + CHAPTER = H1, x.y = H2 (đậm), x.y.z = H3 (thường); markdown _en_v2 đã đúng sẵn; số trang La Mã phải chỉnh tay sau pandoc
- Review intro EN: chỉ ra 15 dấu hiệu văn AI → chốt bộ rule report/QUY_TAC_CHONG_VAN_AI.md + lưu memory áp dụng mọi phiên sau
- Chốt runbook Stop/Start instance (0 lệnh, chỉ verify health) — ghi vào AWS_DEPLOY_ASBUILT.md §12
- Tạo file nhật ký này; quy ước từ nay mỗi việc xong ghi 1 dòng
- Tích hợp Phase 2 AWS: S3 lưu heatmap evidence + CloudWatch log forwarder — commit `98d909b` (storage.py, request_logging.py, push_to_cloudwatch.py + wire 3 router)
- Smoke test ONNX export model SFDCT: PASS (vá `index_add_`→matmul, parity 4e-07, forward nhanh 1.69x) — CHƯA tích hợp vào serving, chờ quyết định
- Tư vấn GPU: đổi sang g4dn.xlarge theo giờ demo (~$0.53/h), cần xin quota G-instance trước
- Đo latency production sau deploy: 4850ms ảnh mới / 22ms cache / 2277ms tắt heatmap
- Tăng tốc inference: warmup model + cache theo sha256 + toggle heatmap có tác dụng thật — commit `4a7eb30`; push bị mạng công ty chặn → Thành push thủ công
- Fix "Phổ tần số không khả dụng": backend thiếu OpenCV → thêm `opencv-python-headless` vào requirements + pin `bcrypt==4.0.1`
- Fix Spatial hiện 0.0% giả: backend trả null, FE ẩn thanh điểm
- PHÁT HIỆN chưa sửa: slider Threshold ở Playground gửi lên nhưng backend bỏ qua (verdict luôn dùng MODEL_THRESHOLD trong .env)
- Hướng dẫn cài SSL Let's Encrypt cho deepguard.ddns.net + rebuild FE với API URL https + sửa CORS
- Fix seed DB rỗng trên EC2: lỗi bcrypt 5.x vs passlib → `pip install bcrypt==4.0.1` rồi `seed.py --full` (5 account demo / Password123!)
- Hướng dẫn xem table Postgres: psql trong Docker hoặc SSH tunnel + DBeaver (không mở port 5432)
- Tư vấn DB: giữ Postgres Docker trên EC2, không dùng RDS; thêm cron pg_dump 2h sáng giữ 7 bản
- Hướng dẫn cài aaPanel trên EC2 (port 7800, mở SG theo My IP, KHÔNG cài LNMP để khỏi sập nginx)
- Chẩn đoán 4 service systemd sau reboot EC2: đều active, web + API healthy từ ngoài
- Áp nội dung slide bảo vệ SFDCT vào template trường (ĐH Bách Khoa ĐN): 17 slide, giữ thiết kế, thay text+17 hình, xoá tên tác giả cũ (slide1 + footer ảnh), nội dung tiếng Anh → Downloads/SLIDE_DATN_SFDCT.pptx
- Thêm bullet "Reading the figure" diễn giải từng hình trên các slide hình (S5-S13), S10/S12 bố cục 2 hình tuần tự + bullet, kèm speaker notes tiếng Việt nêu mục đích show hình (8 slide)
- Chuẩn hoá font slide: Montserrat (tiêu đề) + Open Sans (nội dung) toàn deck; tải static TTF (Open Sans tách từ variable bằng fonttools), NHÚNG đủ Regular+Bold vào pptx (embeddedFontLst) → mở máy hội đồng nào cũng đúng font; file 7.5M
- Soạn kịch bản trình bày 15 phút (workflow 12 agent: đọc CH1-4+slide → outline 5 keypoint → viết nguyên văn VN → kiểm toán đối kháng): report/KICH_BAN_TRINH_BAY_15P.md; vá 4 lỗi logic+5 lỗi trung thực; timing nói thật 2.672 token ~13-15ph
- Tạo hội đồng mô phỏng 3 thành viên (nội dung/hệ thống-demo/method-data-train): report/HOI_DONG_MO_PHONG.md, 32 câu Hỏi-Đáp-Nếu-truy-tiếp; SỬA số rác top-3-avg 0.7507/0.7410 & Row1 0.7333 (từ nháp cũ, ngoài CH4) → căn lại đúng Bảng 4.4 CH4, gắn nhãn ablation cho Row1
- Viết lại kịch bản lối Steve Jobs cho người nói trung-chậm (workflow trim 2.672→~1.900 token, câu ngắn + 84 [DỪNG] + 3 refrain): ghi đè KICH_BAN_TRINH_BAY_15P.md (backup .bak.md). Đồng bộ số gate-α đúng nguồn đo (mean |α| 1.5×10⁻⁴, peak 0.0232, 44/1792 kênh) trên CẢ slide S9 + script + committee; re-export pptx nhúng font
2026-06-18 13:04 — Trim slides Mở đầu (1,2,3) trong report/KICH_BAN_TRINH_BAY_15P.md xuống ~175 token lời nói; cập nhật bảng dòng thời gian (tổng 814s, 13:34); giữ đủ 17 slide, refrain & số liệu khoá nguyên.
- Kịch bản: viết lại văn TỰ NHIÊN (bỏ tật chặt khúc keynote) + bỏ sạch từ "trung thực" + trim về ~11.4 phút (1.611 token, mục tiêu 11-12); giữ số liệu + khung không-SOTA; backup .jobs14min/.natural147/.bak
- Đóng gói nộp source: lọc DeepfakeBench (code+figures, bỏ dataset/.pth/model cũ hfdct/doc nội bộ) + App (be+fe, bỏ node_modules/secret/weight) + 39 hình báo cáo v2_draft → 2 zip ~/Desktop/DeepfakeBench.zip (17M) + App.zip (692K), đã soát 0 rò rỉ

- 2026-06-19 — Soạn `report/SLIDE_CONTENT_MAUDOAN.md`: nội dung từng slide đồ án deepfake bám bố cục mẫu VKU `MauDoAnSlide.pptx` (5 mục: Overview/Goals/System Analysis & Design/Dataset & Model Training/Application Development), ~19 slide, bullet EN + Ý nói VI, hình thật + số liệu khoá.

- 2026-06-20 — Soạn `report/KICH_BAN_DEMO.md`: kịch bản demo bảo vệ bám app thật (Playground=detect deepfake, Liveness, History/Detail), đúng sample presets + tài khoản + START_DEV_STACK; có pre-flight/warm-up, phương án dự phòng video/screenshot, Q&A khi demo.

- 2026-06-20 — Gỡ 3 field vô nghĩa/dễ-bắt-bẻ khỏi API response + UI: `spatial_score`, `frequency_score` (null ở path SFDCT / chỉ là heuristic Laplacian-FFT), `prob_cnn` (trùng prob_fake). Sửa backend schemas/detect.py + routers detect/detections/playground; frontend api.ts, playground/detail/docs-page. Giữ prob_fake (output model) + risk_score. Backend compile OK, frontend tsc 0 lỗi. Giữ DB-write (vô hại).

- 2026-06-20 — Bỏ "challenge" khỏi liveness UI: viết lại `liveness-page.tsx` còn 2 nguồn ảnh Upload/Webcam, cả hai gọi `detect/liveness` passive (bỏ gesture/countdown/challenge_passed do client tự khai). Gỡ doc endpoint active+challenge ở `docs-page.tsx` + 2 dòng Challenge ở `detail-page.tsx`. tsc 0 lỗi. Backend endpoint /detect/liveness/active + api.ts client giữ nguyên (không phải UI).

- 2026-06-20 — Dựng `ekyc_demo/` (Streamlit) mô phỏng dev tích hợp API eKYC: ô nhập API key (ưu tiên ô nhập, trống thì lấy DEEPGUARD_API_KEY ở .env), 3 mode Liveness/Deepfake/eKYC-cascade (liveness trước, LIVE mới chạy deepfake → PASS/REVIEW/FAIL). Gọi /v1/detect/liveness + /v1/detect/image (Bearer key), hiện Grad-CAM/phổ DCT + response JSON + cURL. Kèm .env.example, requirements.txt, README. py_compile OK.

- 2026-06-20 — ekyc_demo: thêm video cho liveness (cv2 trích N khung → gọi /detect/liveness từng khung → gộp majority-vote, mô tả rõ gộp client) + deepfake video qua /detect/video + eKYC cascade hỗ trợ video. Cài streamlit (hạ về 1.49.1 <1.50 vì 1.58 xung đột starlette 0.41 của backend). Smoke test streamlit HTTP 200, không lỗi. (MTCNN crop = backend/serving, không phải FE.)

- 2026-06-20 — Fix "heatmap không khả dụng": cổng 8501 (serving SFDCT) bị `streamlit run app.py` chiếm (streamlit default 8501) → backend rơi mock (heatmap=None, điểm ngẫu nhiên). Tắt streamlit, khởi động lại serving infer_server trên 8501 (model naive-sfdct-cdfv2-0.7572, gradcam OK). Ghim demo Streamlit về cổng 8601 (.streamlit/config.toml) + cảnh báo README. 401 dashboard = chưa đăng nhập (không phải bug).

- 2026-06-20 — Gỡ challenge_type/challenge_passed khỏi LivenessResponse (schema + _to_detail builder + api.ts type); restart backend (giữ env S3). Đáp size input liveness: model fix 256x256 (RESOLUTION ở liveness_server). backend compile OK, tsc 0 lỗi.

- 2026-06-20 — Fix threshold override bị bỏ qua ở deepfake: query `threshold` ở /detect/image + /playground/detect/image nhận nhưng KHÔNG dùng (luôn settings.MODEL_THRESHOLD 0.35). Nối threshold xuyên run_inference → _sfdct/_real/_mock_inference (verdict + threshold_used theo override). Test: thr 0.35→0.35, 0.78→0.78. Restart backend.

- 2026-06-20 — ekyc_demo phương án A: thêm "Lịch sử phiên" (client-side st.session_state, ghi mỗi lần chạy: time/mode/verdict/score/id) + "Tra cứu theo request_id" qua GET /v1/results/{id}. Lý do: API key KHÔNG có endpoint list (/detections cần JWT require_role). py_compile + smoke HTTP 200 OK.

- 2026-06-20 — ekyc_demo nhớ API key: Streamlit không có localStorage (text_input reset khi reload). Thêm nút "Ghi nhớ key vào .env" (ghi DEEPGUARD_API_KEY) + load_dotenv(override=True) để nạp lại mỗi lần. AN TOÀN: thêm ekyc_demo/.env vào .gitignore (trước đó chưa ignore → nguy cơ lộ key). compile + smoke HTTP 200 OK.

- 2026-06-20 — ekyc_demo: thay ô text API URL bằng selectbox Môi trường (Local localhost:8000 / Deploy https://deepguard.ddns.net/api / Custom). Cảnh báo deploy cần prefix /api + key tạo trên deploy. compile + smoke OK.

- 2026-06-20 — ekyc_demo: thêm panel "🔍 Vì sao?" trên màn Streamlit (mở sẵn) cho deepfake (prob_fake vs threshold_used ±0.10 → verdict; risk_score vs band 0.3/0.7 → decision_hint) và liveness (P(live) vs threshold, margin 0 → LIVE/SPOOF). Gỡ 3 log server tạm thêm trước đó (user muốn hiện trên screen). compile + smoke OK.
