# DeepGuard — Test case thủ công theo chức năng (mỗi dòng = 1 chức năng)

> Sinh từ rà soát code thật (router -> service -> crud) + đối chiếu use case + RBAC 5 role.
> Cột **Thực trạng** ghi căn cứ file/endpoint và nêu rõ chỗ mock/gated/chưa chặn (không bịa hành vi).
> Bổ trợ cho `MANUAL_TEST_PLAN.md` (theo trang) và `ROLE_FLOWS.md` (theo actor).
> Tài khoản demo: Password123! - tenant "VietBank Demo" - sysadmin@deepguard.vn / admin@vietbank.vn / dev@vietbank.vn / compliance@vietbank.vn / viewer@vietbank.vn.


## Quản lý Tenant (tổ chức)

| ID | Chức năng | UC | Actor được phép | Tiền điều kiện | Các bước | Kết quả mong đợi (Pass) | Ca từ chối (RBAC/âm) | Thực trạng (căn cứ) |
|---|---|---|---|---|---|---|---|---|
| TC-TEN-01 | Liệt kê toàn bộ tenant xuyên tổ chức (list + phân trang) | - | sysadmin | Đăng nhập bằng sysadmin@deepguard.vn (Password123!); đã có ít nhất 1 tenant trong hệ thống | 1. Đăng nhập sysadmin, lấy JWT \| 2. Mở trang Quản lý Tenants (FE gọi GET /tenants) hoặc curl GET /tenants?page=1&limit=50 với Bearer JWT \| 3. Quan sát bảng/grid danh sách | HTTP 200, trả Paginated{items,total,page,limit}; mỗi item gồm id,name,plan,status,monthly_quota,current_usage,user_count,created_at; sắp xếp giảm dần theo created_at; KHÔNG lọc theo tenant_id (thấy mọi tenant) | Role khác sysadmin (admin/compliance/developer/viewer) -> 403 'System admin access required'; không có JWT -> 401 | platform.py list_all_tenants -> crud.list_all_tenants (offset/limit, join subquery user_count, order_by desc created_at); gated require_sysadmin (dependencies.py). FE tenants-page.tsx tenantsList() = GET /tenants |
| TC-TEN-02 | Tạo tenant mới + tài khoản admin đầu tiên (mật khẩu tạm hiện 1 lần) | - | sysadmin | Đăng nhập sysadmin; email admin dự kiến CHƯA tồn tại trong hệ thống | 1. Bấm 'Tạo tenant', đi qua wizard 3 bước (Tổ chức / Gói&quota / Quản trị viên) \| 2. Nhập tên tổ chức, gói, quota, email admin \| 3. Bấm 'Tạo tenant' (FE POST /tenants {name,admin_email,admin_name?,plan,monthly_quota}) \| 4. Đọc mật khẩu tạm hiển thị, sao chép gửi admin | HTTP 201, trả CreateTenantResponse{tenant, admin_email, temp_password}; tenant tạo ra ở trạng thái ACTIVE ngay (khác self-register); admin mới role ADMIN, must_change_password=True; mật khẩu tạm chỉ hiện 1 lần trên UI | plan không thuộc TenantPlan -> 400 'Invalid plan'; monthly_quota < 0 -> 400; email admin đã có tài khoản -> 409 'Email admin đã có tài khoản'; non-sysadmin -> 403 | platform.py create_tenant_admin: validate plan/quota, check get_user_by_email->409, crud.create_tenant + ép tenant.status=ACTIVE, _gen_temp_password, create_user role ADMIN + must_change_password=True, write_audit_log 'platform.create_tenant'. FE wizard có nhập domain/region/rpm NHƯNG KHÔNG gửi xuống backend (chỉ trang trí) |
| TC-TEN-03 | Duyệt tenant chờ duyệt (pending -> active) / Kích hoạt lại tenant tạm ngưng | - | sysadmin | Đăng nhập sysadmin; tồn tại tenant ở trạng thái pending hoặc suspended | 1. Mở danh sách tenant, tìm tenant pending/suspended \| 2. Bấm nút 'Duyệt' (pending) hoặc 'Kích hoạt' (suspended) \| 3. Xác nhận trong modal (FE PATCH /tenants/{id} {status:'active'}) \| 4. Quan sát badge đổi sang Active | HTTP 200, status tenant chuyển 'active'; audit action='tenant.activated'; danh sách reload phản ánh trạng thái mới | status không thuộc TenantStatus -> 400 'Invalid status'; tenant_id không tồn tại -> 404 'Tenant'; non-sysadmin -> 403 | platform.py update_tenant_admin -> crud.update_tenant_admin (set tenant.status); audit verb-map: status=='active' -> 'tenant.activated'. FE statusActionLabel: pending->'Duyệt', suspended->'Kích hoạt'; cả hai đều gửi status:'active' |
| TC-TEN-04 | Tạm ngưng tenant (active -> suspended) | - | sysadmin | Đăng nhập sysadmin; tồn tại tenant đang active | 1. Mở danh sách tenant active \| 2. Bấm nút 'Suspend' \| 3. Xác nhận trong modal cảnh báo 'tất cả API keys sẽ bị ngưng' (FE PATCH /tenants/{id} {status:'suspended'}) \| 4. Quan sát badge Suspended | HTTP 200, status tenant chuyển 'suspended'; audit action='tenant.suspended' (FE tô màu warning) | status sai -> 400; tenant không tồn tại -> 404; non-sysadmin -> 403. LƯU Ý: code KHÔNG chặn sysadmin tự suspend tenant của chính mình — không có guard so sánh current_user.tenant_id, sysadmin xuyên tenant nên thao tác vẫn thành công (rủi ro tự khóa, chưa được chặn) | platform.py update_tenant_admin: audit verb 'tenant.suspended'; KHÔNG có kiểm tra tenant_id==current_user.tenant_id. require_sysadmin chỉ check role, không ràng buộc tenant. FE modal chỉ cảnh báo text, không chặn tenant của sysadmin |
| TC-TEN-05 | Đổi gói cước (plan) của tenant | - | sysadmin | Đăng nhập sysadmin; chọn 1 tenant trong danh sách | 1. Mở dòng tenant -> bung chi tiết (drill-down) \| 2. Trong hàng 'Đổi gói', bấm Starter/Pro/Enterprise (khác gói hiện tại) \| 3. FE PATCH /tenants/{id} {plan:'pro'} (lowercase) \| 4. Quan sát PlanBadge cập nhật | HTTP 200, tenant.plan đổi sang giá trị mới; audit action='platform.update_tenant' | plan không thuộc TenantPlan -> 400 'Invalid plan'; tenant không tồn tại -> 404; non-sysadmin -> 403; chọn đúng gói hiện tại -> FE bỏ qua (nút disabled, không gọi API) | platform.py update_tenant_admin set tenant.plan; audit verb else-branch -> 'platform.update_tenant'. FE changePlan() bỏ qua nếu next===t.plan, gửi plan.toLowerCase() |
| TC-TEN-06 | Điều chỉnh quota tháng (monthly_quota) của tenant | - | sysadmin | Đăng nhập sysadmin; chọn 1 tenant | 1. Bung chi tiết tenant \| 2. Bấm 'Chỉnh quota' \| 3. Nhập số request mới vào prompt \| 4. FE PATCH /tenants/{id} {monthly_quota:N} \| 5. Quan sát thanh quota cập nhật | HTTP 200, tenant.monthly_quota = N; audit action='platform.update_tenant' | monthly_quota < 0 -> 400 'monthly_quota must be >= 0'; nhập không phải số/âm -> FE chặn tại chỗ ('Quota không hợp lệ', không gọi API); tenant không tồn tại -> 404; non-sysadmin -> 403 | platform.py validate monthly_quota>=0 -> bad_request; crud set tenant.monthly_quota. FE adjustQuota() window.prompt + parseInt, NaN/âm -> setActionError không gọi API |
| TC-TEN-07 | Xem KPI tổng quan danh sách tenant (tổng/đang hoạt động/usage/users) | - | sysadmin | Đăng nhập sysadmin; đã tải danh sách tenant | 1. Mở trang Quản lý Tenants \| 2. Quan sát 4 ô StatPill phía trên (Tổng tenants, Đang hoạt động, Tổng usage, Tổng người dùng) | 4 chỉ số hiển thị đúng: total = số tenant, active = đếm status==active, totalUsage = tổng current_usage, totalUsers = tổng user_count — tất cả tính phía FE từ danh sách GET /tenants | non-sysadmin không vào được trang (GET /tenants -> 403) | FE tenants-page.tsx useMemo kpis tính client-side từ mảng tenants (không gọi endpoint riêng). LƯU Ý: khác endpoint GET /platform/overview (crud.platform_overview) dùng cho Platform Console, KHÔNG dùng ở trang này |
| TC-TEN-08 | Drill-down danh sách người dùng của một tenant bất kỳ (xuyên tenant) | - | sysadmin | Đăng nhập sysadmin; chọn 1 tenant để bung chi tiết | 1. Bấm 'View'/expand một tenant \| 2. FE lazy-load GET /tenants/{id}/users \| 3. Quan sát panel 'Người dùng' | HTTP 200, list TenantUserItem (id,email,name,role,is_active,last_login_at,created_at) của tenant đó, loại user đã xóa (deleted_at null), sắp xếp giảm dần created_at; FE cache theo tenant id | tenant_id không tồn tại -> 404 'Tenant'; non-sysadmin -> 403; JWT thiếu -> 401 | platform.py tenant_users: check get_tenant->404, query User where tenant_id & deleted_at is null order desc created_at. FE loadDrill() Promise.all([tenantUsers,tenantApiKeys]) |
| TC-TEN-09 | Drill-down danh sách API key của một tenant bất kỳ (read-only, xuyên tenant) | - | sysadmin | Đăng nhập sysadmin; bung chi tiết 1 tenant | 1. Expand tenant \| 2. FE lazy-load GET /tenants/{id}/api-keys \| 3. Quan sát panel 'API Keys' | HTTP 200, list TenantApiKeyItem (name,prefix,status,quota_limit,quota_used,rate_limit_rpm,last_used_at,created_at); panel chỉ đọc (FE không có nút sửa/thu hồi tại đây) | tenant_id không tồn tại -> 404 'Tenant'; non-sysadmin -> 403 | platform.py tenant_api_keys: check get_tenant->404, crud.list_api_keys(tenant_id) (lọc deleted_at null). FE ApiKeysPanel read-only, chỉ hiển thị prefix+'••••••' |
| TC-TEN-10 | Đổi vai trò / kích hoạt-tạm ngưng thành viên của tenant khác (cross-tenant) | - | sysadmin | Đăng nhập sysadmin; bung chi tiết tenant; có user trong panel 'Người dùng' | 1. Expand tenant -> panel Người dùng \| 2. Đổi role qua dropdown (viewer/developer/compliance/admin) hoặc bấm nút Active/Tạm ngưng \| 3. FE PATCH /tenants/{id}/users/{uid} {role?\|is_active?} \| 4. Quan sát hàng cập nhật | HTTP 200, trả TenantUserItem đã cập nhật role/is_active; audit action='platform.update_tenant_user' | tự sửa chính mình (user.id==current_user.id) -> 400 'Dùng trang Tài khoản để tự đổi thông tin của mình' (FE cũng khóa hàng BẠN); gán role='sysadmin' -> 400 'Không thể gán vai trò sysadmin cho thành viên tenant'; role không hợp lệ -> 400; user/tenant không khớp -> 404 'User'; non-sysadmin -> 403 | platform.py update_tenant_user: query user theo id+tenant_id+deleted_at null ->404; chặn self-edit ->400; validate role + chặn 'sysadmin' ->400; set role/is_active; write_audit_log 'platform.update_tenant_user'. FE UserRow khóa select khi isSelf hoặc role==sysadmin; EDITABLE_ROLES loại sysadmin |
| TC-TEN-11 | Xem biểu đồ lưu lượng 7 ngày theo tenant (chi tiết drill-down) | - | sysadmin | Đăng nhập sysadmin; bung chi tiết 1 tenant | 1. Expand tenant \| 2. Nhìn ô 'Lưu lượng 7 ngày' trong khối chi tiết | Hiển thị placeholder 'chi tiết theo tenant — sắp có' (hourglass), KHÔNG có dữ liệu time-series; đây là tính năng chưa triển khai | - | FE tenants-page.tsx component ComingSoon trong TenantDetail: 'Lưu lượng 7 ngày' -> không có endpoint time-series theo tenant; chú thích trong code 'Time-series traffic has no endpoint yet → kept as sắp có' |


## Quản lý API Key

| ID | Chức năng | UC | Actor được phép | Tiền điều kiện | Các bước | Kết quả mong đợi (Pass) | Ca từ chối (RBAC/âm) | Thực trạng (căn cứ) |
|---|---|---|---|---|---|---|---|---|
| TC-KEY-01 | Tạo API key (hiện plain key 1 lần) | - | admin, developer (cùng tenant). sysadmin KHÔNG nằm trong require_role nên bị 403. | Đã đăng nhập dashboard (JWT Bearer), role admin hoặc developer, tenant đang active. | 1. Đăng nhập dashboard bằng admin@vietbank.vn hoặc dev@vietbank.vn (Password123!) \| 2. Vào trang API Keys, bấm 'Tạo key mới' \| 3. Nhập Tên key (bắt buộc), Quota (mặc định 10000), Rate limit (mặc định 100 rpm), bấm 'Tạo key' \| 4. Hoặc curl: POST /api-keys với header Authorization: Bearer <JWT>, body {"name":"Prod","quota_limit":10000,"rate_limit_rpm":100} | HTTP 201, trả ApiKeyCreated gồm id, name, prefix, status=active, quota_limit, quota_used=0, rate_limit_rpm và plain_key (chuỗi sk-dg-... đầy đủ) — plain_key CHỈ trả 1 lần. UI hiện modal reveal có nút Hiện/Ẩn + Copy rồi để lại banner xanh nhắc copy; key tạo cũng được lưu vào auth store (setApiKey). Lần list sau chỉ thấy prefix, không bao giờ thấy lại plain_key. Ghi audit action=api_key.created. | role compliance/viewer -> 403 'Requires role: admin, developer'; sysadmin -> 403 (không thuộc allowed set); chưa đăng nhập -> 401; tenant suspended -> 403 'Tổ chức đã bị tạm ngưng' (chặn ở get_current_user); tên rỗng -> UI disable nút (frontend chặn, backend không validate name rỗng). | backend/app/routers/api_keys.py::create_api_key (POST /api-keys, require_role('admin','developer')), crud.create_api_key sinh plain qua _generate_api_key, trả prefix + key_hash (không lưu plain). Schema ApiKeyCreated có plain_key (backend/app/schemas/api_keys.py). UI: apikeys-page.tsx handleCreate -> apiKeysCreate (lib/api.ts), 2 step config/reveal. LƯU Ý: backend KHÔNG kiểm tra quota_limit/rate_limit hợp lệ lúc tạo (chỉ validate ở PATCH); UI tự ép tối thiểu 1 bằng Math.max. |
| TC-KEY-02 | Liệt kê API key của tenant (list) | - | admin, developer (cùng tenant). | Đã đăng nhập dashboard, role admin/developer. | 1. Đăng nhập dashboard \| 2. Mở trang API Keys (tự gọi list khi load) \| 3. Hoặc curl: GET /api-keys với Authorization: Bearer <JWT> | HTTP 200, mảng ApiKeyOut sắp xếp created_at giảm dần, CHỈ gồm key của tenant người dùng và chưa bị xoá mềm (deleted_at null). Mỗi item: id, name, prefix (không có plain_key), status, quota_limit, quota_used, rate_limit_rpm, last_used_at, expires_at, created_at. UI hiển thị bảng/thẻ + KPI (tổng keys, đang active, requests đã dùng = tổng quota_used, % quota tổng) và lọc theo trạng thái/từ khoá ở client. | role compliance/viewer/sysadmin -> 403; chưa đăng nhập -> 401; tenant suspended -> 403. Không thấy key của tenant khác (lọc theo current_user.tenant_id). | backend/app/routers/api_keys.py::list_api_keys (GET /api-keys), crud.list_api_keys lọc tenant_id + deleted_at IS NULL + order_by desc(created_at). UI: apikeys-page.tsx loadKeys -> apiKeysList (lib/api.ts). KPI 'requests đã dùng' và sparkline 7 ngày là suy ra từ quota_used (sparkFor là dữ liệu giả tất định seed theo id, KHÔNG phải usage thật theo ngày). |
| TC-KEY-03 | Xem chi tiết 1 API key (get by id) | - | admin, developer (cùng tenant). | Đã đăng nhập dashboard, role admin/developer, biết key_id. | 1. Đăng nhập dashboard \| 2. curl: GET /api-keys/{key_id} với Authorization: Bearer <JWT> | HTTP 200, trả ApiKeyOut của key đó (chỉ prefix, không bao giờ plain key). Nếu key_id không thuộc tenant của mình hoặc đã xoá mềm -> 404 'API key not found'. | key_id của tenant khác -> 404 (IDOR bị chặn bằng điều kiện tenant_id == current_user.tenant_id); key đã revoke/xoá mềm -> 404; role compliance/viewer/sysadmin -> 403; chưa đăng nhập -> 401. | backend/app/routers/api_keys.py::get_api_key (GET /api-keys/{key_id}), query trực tiếp ApiKey lọc id + tenant_id + deleted_at IS NULL. CÓ client apiKeysGet trong lib/api.ts NHƯNG trang apikeys-page.tsx KHÔNG gọi (không có màn chi tiết) — endpoint dùng được qua Swagger/curl. |
| TC-KEY-04 | Cập nhật API key (đổi tên/quota/rate-limit/tạm ngưng) | - | admin, developer (cùng tenant). | Đã đăng nhập dashboard, role admin/developer, biết key_id. | 1. Đăng nhập dashboard \| 2. curl: PATCH /api-keys/{key_id} với Authorization: Bearer <JWT>, body ví dụ {"name":"New","quota_limit":20000,"rate_limit_rpm":200,"status":"suspended"} | HTTP 200, trả ApiKeyOut sau cập nhật. Chỉ field được gửi mới đổi (Optional). status hợp lệ: active\|suspended (suspended sẽ làm validate_api_key trả None -> key hết auth được). Ghi audit action=api_key.updated. | status='revoked' -> 400 'Use DELETE /api-keys/{id} to revoke'; quota_limit < 0 -> 400; rate_limit_rpm <= 0 -> 400; status lạ -> 400 'Invalid status'; key_id tenant khác/đã xoá -> 404; role compliance/viewer/sysadmin -> 403; chưa đăng nhập -> 401. | backend/app/routers/api_keys.py::update_api_key (PATCH /api-keys/{key_id}), schema UpdateApiKeyRequest (backend/app/schemas/api_keys.py). CÓ client apiKeysUpdate trong lib/api.ts NHƯNG trang apikeys-page.tsx KHÔNG có UI sửa (chỉ có Tạo + Thu hồi) -> dùng được qua Swagger/curl. Trên UI có badge 'rotating'/'suspended' nhưng không có thao tác đổi sang chúng. |
| TC-KEY-05 | Thu hồi / xoá API key (revoke) | - | admin, developer (cùng tenant). | Đã đăng nhập dashboard, role admin/developer, key chưa ở trạng thái revoked. | 1. Đăng nhập dashboard \| 2. Trang API Keys, bấm menu '...' của key -> 'Thu hồi', xác nhận hộp confirm \| 3. Hoặc curl: DELETE /api-keys/{key_id} với Authorization: Bearer <JWT> | HTTP 204 (no content). Key bị xoá mềm: status=REVOKED + deleted_at được set, sau đó không còn auth được (validate_api_key chỉ nhận status ACTIVE và deleted_at null) và không còn xuất hiện ở list. Ghi audit action=api_key.revoked. UI gọi xong sẽ load lại danh sách; nút 'Thu hồi' bị disable nếu key đã revoked (canRevoke=false). | key_id tenant khác/đã xoá -> 404 (IDOR chặn); role compliance/viewer/sysadmin -> 403; chưa đăng nhập -> 401; tenant suspended -> 403. | backend/app/routers/api_keys.py::revoke_api_key (DELETE /api-keys/{key_id}), crud.revoke_api_key set status=REVOKED + deleted_at. UI: apikeys-page.tsx handleRevoke -> confirm() -> apiKeysRevoke (lib/api.ts) -> loadKeys(). LƯU Ý: lỗi revoke bị nuốt (.catch(()=>{})), UI không báo lỗi cho người dùng. |
| TC-KEY-06 | Thực thi quota & rate-limit khi gọi API /v1 bằng API key | - | External eKYC client (dùng API-key Bearer sk-dg-...), không phải user dashboard. | Có plain API key hợp lệ (status active, chưa hết hạn), tenant active. | 1. Gọi endpoint tích hợp /v1/* với header Authorization: Bearer sk-dg-... \| 2. Lặp lại đến khi quota_used >= quota_limit để kiểm tra chặn | Key hợp lệ -> qua get_api_key_auth (validate bằng hash, gắn tenant). Nếu quota_limit > 0 và quota_used >= quota_limit -> trả lỗi quota_exceeded (429). quota_limit = 0 nghĩa là không giới hạn (bỏ qua kiểm tra). Trang API Keys chỉ HIỂN THỊ quota_used/quota_limit và % dùng, không đặt lại usage được từ UI. | key sai/không tồn tại -> 401 'Invalid API key'; key đã revoke/suspended/hết hạn -> 401 (validate_api_key trả None); tenant suspended -> 403 'Tổ chức đã bị tạm ngưng'; vượt quota -> 429 quota_exceeded. | backend/app/dependencies.py::get_api_key_auth + crud.validate_api_key (kiểm status ACTIVE, deleted_at null, expires_at). Đây là lớp auth THỨ HAI (API-key), tách khỏi router quản lý key (dùng JWT). rate_limit_rpm được lưu/hiển thị nhưng trong get_api_key_auth chưa thấy code đếm theo phút để chặn — chỉ quota tổng được enforce; rate-limit thực tế phụ thuộc tầng khác, chưa enforce trong dependency này. |


## Quản lý User (developer, viewer, compliance, admin)

| ID | Chức năng | UC | Actor được phép | Tiền điều kiện | Các bước | Kết quả mong đợi (Pass) | Ca từ chối (RBAC/âm) | Thực trạng (căn cứ) |
|---|---|---|---|---|---|---|---|---|
| TC-USR-01 | Xem danh sách thành viên trong tenant | - | admin, sysadmin | Đã đăng nhập JWT, role admin/sysadmin; có ít nhất 1 user trong tenant | 1. Đăng nhập admin@vietbank.vn / Password123! \| 2. Vào menu Team & Roles, tab 'Thành viên' \| 3. Quan sát bảng members; hoặc gọi GET /users với Bearer JWT \| 4. Dùng ô tìm kiếm tên/email và chip lọc theo vai trò | GET /users trả 200 + danh sách user của ĐÚNG tenant hiện tại (loại bản ghi đã soft-delete deleted_at), sắp xếp created_at giảm dần, kèm total; UI hiển thị tên/email/vai trò/trạng thái/last_login/ngày tham gia | Role developer/viewer/compliance -> 403 'Admin role required' (_require_admin chỉ cho admin/sysadmin); chưa đăng nhập -> 401 | list_users() trong users.py lọc tenant_id==current_user.tenant_id và deleted_at IS NULL; _require_admin chặn non-admin. FE usersList() -> GET /users. Danh sách CHỈ trong tenant (cô lập tenant đúng) |
| TC-USR-02 | Thêm nhân viên trực tiếp (active ngay, gán role) | - | admin (role <= mình, không tạo compliance/sysadmin), sysadmin (mọi role) | Đăng nhập admin/sysadmin; email chưa tồn tại trong tenant; mật khẩu >= 8 ký tự | 1. Bấm 'Thêm nhân viên' \| 2. Nhập họ tên, email, chọn vai trò (chỉ các role được phép gán), nhập hoặc bấm 'Tạo ngẫu nhiên' cho mật khẩu \| 3. Bấm 'Tạo nhân viên' (FE usersCreate) -> POST /users {email,name,role,password} \| 4. Kiểm tra nhân viên xuất hiện trong bảng, trạng thái Active | POST /users trả 201 + UserListItem; user tạo trong tenant hiện tại, active ngay, must_change_password=True (buộc đổi mật khẩu lần đăng nhập đầu); ghi audit user.created | Email trùng trong tenant -> 409 'Email đã tồn tại trong tổ chức'; admin chọn role compliance/sysadmin -> 403 (_can_assign chặn ADMIN_PROTECTED_ROLES); role chuỗi sai -> 400 'Invalid role'; non-admin -> 403 | create_user_direct() trong users.py: _require_admin + _can_assign (admin không gán compliance/sysadmin) + kiểm tra email trùng; đặt user.must_change_password=True. FE form validate name/email/password>=8 client-side trước khi gọi |
| TC-USR-03 | Mời thành viên qua link (Invitation token) | - | admin (role được phép gán), sysadmin | Đăng nhập admin/sysadmin; email chưa thuộc user nào trong tenant | 1. Gọi POST /users/invite {email,name,role} qua Swagger/curl với Bearer JWT (FE có hàm usersInvite nhưng trang Team hiện chỉ dùng tạo trực tiếp) \| 2. Nhận response invite_url dạng /?invite=<token> \| 3. Gửi link cho người được mời; họ mở SPA, FE phát hiện ?invite= -> trang accept-invite | POST /users/invite trả 201 + {invitation_id,email,role,token,expires_at,invite_url='/?invite=<token>'}; Invitation gắn tenant hiện tại, hết hạn sau 7 ngày (INVITATION_TTL_DAYS); ghi audit user.invited | Email đã có user trong tenant -> 409 'User with this email already exists in tenant'; admin mời role compliance/sysadmin -> 403; role sai -> 400 'Invalid role'; non-admin -> 403 | invite_user() trong users_invites.py tạo Invitation token=secrets.token_urlsafe(48), expires=now+7d. invite_url chỉ là path SPA, KHÔNG gửi email (FE tự phát hiện ?invite=). team-page.tsx KHÔNG render nút mời -> luồng invite chỉ truy cập qua API client usersInvite / endpoint |
| TC-USR-04 | Đổi vai trò thành viên | - | admin (target không phải compliance/sysadmin, gán role không phải compliance/sysadmin), sysadmin (mọi role) | Đăng nhập admin/sysadmin; target user thuộc cùng tenant, chưa bị xóa | 1. Mở Team & Roles, click 1 thành viên (mở drawer) hoặc dùng dropdown vai trò ở dòng \| 2. Chọn vai trò mới trong select \| 3. FE usersUpdate(id,{role}) -> PATCH /users/{id} {role} \| 4. Quan sát badge vai trò cập nhật | PATCH /users/{id} trả 200 + UserListItem với role mới; ghi audit user.updated | Đổi role chính mình -> 400 'Không thể tự đổi vai trò hoặc vô hiệu hóa chính mình'; admin sửa target compliance/sysadmin -> 403; admin gán role compliance/sysadmin -> 403 (_can_assign); user khác tenant -> 404 'User'; role sai -> 400; non-admin -> 403 | update_user() trong users.py: _require_admin + _can_manage(target) + chặn tự đổi role/tự vô hiệu hóa (user.id==current_user.id) + _can_assign(new_role). FE canManageUser/assignableRoles ẩn dropdown với target cao hơn hoặc dòng của chính mình (chỉ hiện badge tĩnh + icon khóa) |
| TC-USR-05 | Bật/tắt (tạm ngưng / kích hoạt) thành viên | - | admin (target không phải compliance/sysadmin), sysadmin | Đăng nhập admin/sysadmin; target cùng tenant, chưa xóa | 1. Mở drawer chi tiết thành viên \| 2. Bấm 'Tạm ngưng truy cập' hoặc 'Kích hoạt lại' \| 3. FE usersUpdate(id,{is_active}) -> PATCH /users/{id} {is_active} \| 4. Quan sát badge Active/Inactive đổi | PATCH /users/{id} trả 200, is_active đặt theo giá trị gửi lên; ghi audit user.updated kèm is_active | Tự vô hiệu hóa chính mình (is_active=False trên dòng của mình) -> 400 'Không thể tự đổi vai trò hoặc vô hiệu hóa chính mình'; admin tắt target compliance/sysadmin -> 403; user khác tenant -> 404; non-admin -> 403 | update_user() áp dụng body.is_active; điều kiện chặn tự khóa: user.id==current_user.id and body.is_active is False -> bad_request. FE toggleActive() chặn isSelf trước khi gọi; nút chỉ hiện khi manageable (không phải self, không phải role cao hơn) |
| TC-USR-06 | Xóa (soft-delete / gỡ) thành viên khỏi workspace | - | admin (target không phải compliance/sysadmin), sysadmin | Đăng nhập admin/sysadmin; target cùng tenant, chưa xóa; target khác chính mình | 1. Mở drawer chi tiết thành viên \| 2. Bấm 'Gỡ khỏi workspace' \| 3. FE usersDelete(id) -> DELETE /users/{id} \| 4. Quan sát thành viên biến mất khỏi danh sách | DELETE /users/{id} trả 204; user set deleted_at=now + is_active=False (soft-delete, không xóa cứng); ghi audit user.deleted; list_users sẽ không còn hiển thị | Tự xóa chính mình -> 400 'Cannot delete yourself'; admin xóa target compliance/sysadmin -> 403; user khác tenant hoặc đã xóa -> 404 'User'; non-admin -> 403 | delete_user() trong users.py: chặn user_id==current_user.id trước, rồi _require_admin + _can_manage; set deleted_at + is_active=False. FE removeMember() chặn isSelf; nút chỉ hiện khi manageable |
| TC-USR-07 | Reset mật khẩu thành viên (mật khẩu tạm 1 lần + buộc đổi) | - | admin (target không phải compliance/sysadmin), sysadmin | Đăng nhập admin/sysadmin; target cùng tenant, chưa xóa; target khác chính mình | 1. Bấm icon 'Đặt lại mật khẩu' ở dòng hoặc nút trong drawer \| 2. Xác nhận trong modal 'Đặt lại mật khẩu' \| 3. FE usersResetPassword(id) -> POST /users/{id}/reset-password \| 4. Đọc mật khẩu tạm hiển thị 1 lần trong panel, bấm 'Sao chép', cấp cho nhân viên | POST /users/{id}/reset-password trả 200 {temp_password, must_change_password:true}; password_hash đặt lại bằng mật khẩu tạm ngẫu nhiên 12 ký tự (đủ thường/HOA/số), must_change_password=True; ghi audit user.reset_password. Admin KHÔNG biết/đặt mật khẩu cố định của member; UI cảnh báo mật khẩu chỉ hiện 1 lần | Tự reset chính mình -> 400 'Dùng trang Tài khoản để tự đổi mật khẩu'; admin reset target compliance/sysadmin -> 403; user khác tenant -> 404; non-admin -> 403 | reset_member_password() trong users_invites.py: _require_admin + _can_manage + chặn self; _gen_temp_password() sinh mật khẩu 12 ký tự đảm bảo có chữ thường/HOA/số; trả temp ra response (hiện 1 lần). FE TempPasswordPanel hiển thị + CopyButton, không lưu lại |


## Xác thực & phiên

| ID | Chức năng | UC | Actor được phép | Tiền điều kiện | Các bước | Kết quả mong đợi (Pass) | Ca từ chối (RBAC/âm) | Thực trạng (căn cứ) |
|---|---|---|---|---|---|---|---|---|
| TC-AUTH-01 | Đăng nhập dashboard (login đúng thông tin) | - | anonymous (mọi role sau đó: sysadmin, admin, compliance, developer, viewer) | Tenant ở trạng thái active; user is_active=true, chưa bị xóa mềm; đã có tài khoản (vd dev@vietbank.vn / Password123!). | 1. Mở trang Đăng nhập (login-page.tsx) \| 2. Nhập email + mật khẩu \| 3. Bấm 'Đăng nhập' (FE gọi POST /auth/login) \| 4. FE lưu access_token vào localStorage 'dg_token', gọi GET /auth/me lấy user+tenant \| 5. Điều hướng tới trang mặc định theo role (defaultPageFor) | POST /auth/login trả 200 + {access_token} (JWT); cập nhật last_login; ghi audit 'user.login'. FE hiện màn 'Đăng nhập thành công' rồi chuyển hướng dashboard đúng role. | Sai email/mật khẩu -> 401 'Invalid email or password'; user is_active=false -> 401 'Account is disabled'. FE rung form + hiện thông báo lỗi đỏ. | Đúng code backend/app/routers/auth.py::login: verify_password, check is_active, check tenant.status=='active', create_access_token. FE login-page.tsx::handleLogin gọi authLogin->authMe->setAuth (lib/api.ts:47). |
| TC-AUTH-02 | Đăng nhập khi tổ chức (tenant) bị tạm ngưng/chờ duyệt | - | anonymous (admin của tenant chưa được duyệt) | Đã có user hợp lệ nhưng tenant.status = pending (vừa tự đăng ký) hoặc suspended. | 1. Mở trang Đăng nhập \| 2. Nhập email + mật khẩu đúng \| 3. Bấm 'Đăng nhập' (POST /auth/login) | Trả 401: nếu tenant.status=='pending' -> 'Tổ chức đang chờ quản trị nền tảng phê duyệt.'; nếu suspended/khác active -> 'Tổ chức đã bị tạm ngưng. Liên hệ quản trị nền tảng.' Không cấp token. | Đây chính là ca từ chối: tenant pending/suspended -> 401 (KHÔNG trả access_token). | backend/app/routers/auth.py::login kiểm tra tenant.status.value != 'active' -> raise unauthorized với 2 thông điệp phân biệt pending/suspended. Lưu ý: đăng nhập dùng 401 (unauthorized), KHÁC với get_current_user dùng 403. |
| TC-AUTH-03 | Đăng ký tổ chức B2B tự phục vụ (register -> tenant PENDING chờ duyệt) | - | anonymous | Email chưa được đăng ký trong hệ thống; mật khẩu >= 8 ký tự (FE chặn). | 1. Mở trang Đăng ký (register-page.tsx) \| 2. Nhập tên tổ chức, họ tên admin, email, mật khẩu \| 3. Bấm 'TẠO TỔ CHỨC' (POST /auth/register) \| 4. Xem màn 'Đã gửi đăng ký - chờ duyệt' | POST /auth/register trả 201 + {message, tenant_id}; tạo tenant ở status PENDING và user role 'admin'. KHÔNG trả access_token, KHÔNG auto-login. FE hiện màn chờ duyệt, nút 'Về trang đăng nhập'. | Email đã tồn tại -> 409 'Email already registered'. FE hiện thông báo lỗi đỏ. | backend/app/routers/auth.py::register: get_user_by_email -> conflict nếu trùng; create_tenant rồi gán tenant.status=TenantStatus.PENDING; create_user role='admin'; trả RegisterPendingResponse (không có token). FE register-page.tsx::submit (authRegister, lib/api.ts:51). |
| TC-AUTH-04 | Xem thông tin lời mời trước khi chấp nhận (validate invite token) | - | anonymous (người được mời mở link /?invite=<token>) | Có URL chứa ?invite=<token> do admin tenant gửi. | 1. Mở link mời /?invite=<token> (accept-invite-page.tsx đọc token từ URL) \| 2. FE tự gọi GET /auth/accept-invite?token=... \| 3. Nếu hợp lệ -> hiện email/role/tên tổ chức + form đặt mật khẩu | GET /auth/accept-invite trả 200 + {valid:true, email, role, tenant_name} nếu token hợp lệ; nếu không -> {valid:false, reason} với reason: 'Lời mời không tồn tại' / 'Lời mời đã được sử dụng' / 'Lời mời đã hết hạn'. FE hiện màn 'Lời mời không hợp lệ' khi invalid. | Token thiếu/sai/đã dùng/hết hạn -> trả valid:false + lý do (HTTP 200, không lỗi); FE hiện màn lỗi với nút về đăng nhập. | backend/app/routers/auth.py::invite_info + _invitation_state kiểm tra inv None / accepted_at / expires_at. FE accept-invite-page.tsx useEffect gọi authInviteInfo (lib/api.ts:56). Endpoint GET dùng response valid/reason, KHÔNG raise. |
| TC-AUTH-05 | Chấp nhận lời mời tham gia tổ chức (accept-invite -> tạo user theo role + auto-login) | - | anonymous -> trở thành user theo role được mời (admin/compliance/developer/viewer) | Token mời còn hợp lệ (chưa dùng, chưa hết hạn); email chưa có tài khoản; mật khẩu >= 8 ký tự (FE chặn). | 1. Ở màn lời mời hợp lệ, nhập họ tên + mật khẩu \| 2. Bấm 'THAM GIA & ĐĂNG NHẬP' (POST /auth/accept-invite) \| 3. FE lưu token vào 'dg_token', gọi GET /auth/me \| 4. Xóa ?invite khỏi URL và điều hướng vào dashboard theo role | POST /auth/accept-invite trả 201 + {access_token}; tạo user với role = role trong lời mời; đánh dấu invitation.accepted_at (tiêu thụ lời mời); ghi audit 'user.accept_invite'; FE auto-login vào dashboard. | Token invalid/đã dùng/hết hạn -> 400 (bad_request với lý do tương ứng); email đã có tài khoản -> 409 'Email đã có tài khoản'. | backend/app/routers/auth.py::accept_invite: _invitation_state -> bad_request; get_user_by_email -> conflict; create_user role=inv.role; set accepted_at; create_access_token. FE accept-invite-page.tsx::submit (authAcceptInvite, lib/api.ts:58). |
| TC-AUTH-06 | Buộc đổi mật khẩu lần đầu (must_change_password chặn mọi route khác) | - | Mọi user vừa được sysadmin/admin reset mật khẩu (must_change_password=true) | Đã đăng nhập thành công nhưng user.must_change_password=true (vd sau khi admin reset-password cấp temp_password). | 1. Đăng nhập bằng mật khẩu tạm \| 2. FE (app/page.tsx) phát hiện user.must_change_password -> render màn ForceChangePassword, chặn vào dashboard \| 3. Nhập mật khẩu tạm + mật khẩu mới (>=8, khác mật khẩu cũ) + xác nhận \| 4. Bấm 'ĐỔI MẬT KHẨU' (POST /auth/change-password) \| 5. FE gọi lại GET /auth/me, cờ clear -> vào dashboard | Khi must_change_password=true: backend chặn mọi route trừ /auth/change-password, /auth/me, /auth/logout (trả 403 'Bạn phải đổi mật khẩu trước khi tiếp tục'). POST /auth/change-password trả 204, set must_change_password=false, ghi audit 'user.change_password'. Sau đó truy cập bình thường. | Mật khẩu hiện tại sai -> 400 'Mật khẩu hiện tại không đúng'; mật khẩu mới < 8 ký tự -> 400. Khi đang gated mà gọi API khác (vd /detect) -> 403 must_change_password. FE còn chặn: new!=confirm, new==current. | backend/app/dependencies.py::get_current_user: _MUST_CHANGE_ALLOWED + raise forbidden khi path ngoài danh sách. auth.py::change_password verify mật khẩu cũ, set must_change_password=False. FE app/page.tsx:160 render ForceChangePassword; force-change-password.tsx::handleSubmit (authChangePassword, lib/api.ts:69). |
| TC-AUTH-07 | Đăng xuất (logout) | - | Mọi user đã đăng nhập (sysadmin, admin, compliance, developer, viewer) | Đang có phiên đăng nhập hợp lệ (token trong localStorage 'dg_token'). | 1. Bấm nút 'Đăng xuất' trên UI (vd trong ForceChangePassword hoặc menu) \| 2. FE gọi store.logout() xóa 'dg_token' và state \| 3. Điều hướng về trang đăng nhập | Phiên kết thúc phía client: localStorage 'dg_token' bị xóa, không còn truy cập dashboard. Token cũ (JWT stateless) vẫn hợp lệ tới khi hết hạn vì backend không thu hồi. | - | JWT stateless. FE logout = client-side clear: store/auth.ts logout() removeItem('dg_token'); force-change-password.tsx::handleLogout gọi logout()+navigate('login'). Backend CÓ POST /auth/logout (auth.py::logout) chỉ ghi audit 'user.logout', nhưng FE (lib/api.ts) KHÔNG có wrapper authLogout nên không gọi endpoint này -> logout thực tế chỉ là xóa token phía client. |
| TC-AUTH-08 | Làm mới access token (refresh) | - | Mọi user đã đăng nhập | Có JWT hiện tại còn giải mã được; user.is_active=true. | 1. Gọi POST /auth/refresh kèm Bearer JWT hiện tại (qua Swagger/curl) \| 2. Nhận access_token mới với expiry mới | POST /auth/refresh trả 200 + {access_token} mới (expiry mới) cho long-lived session. | Token sai/hết hạn -> 401 'Invalid or expired token' (qua get_current_user); user is_active=false -> 401 'Account is disabled'. | backend/app/routers/auth.py::refresh_token phụ thuộc get_current_user rồi create_access_token. LƯU Ý: FE (lib/api.ts) hiện KHÔNG có hàm gọi /auth/refresh -> endpoint tồn tại nhưng chưa được dashboard sử dụng (chỉ test qua Swagger/curl). |
| TC-AUTH-09 | Xem hồ sơ phiên hiện tại (GET /auth/me) | - | Mọi user đã đăng nhập | Có JWT hợp lệ; tenant active. | 1. Sau đăng nhập, FE tự gọi GET /auth/me \| 2. Nhận {user, tenant} để dựng context + RBAC trên UI | GET /auth/me trả 200 + {user, tenant}. Được phép cả khi must_change_password=true (nằm trong allowlist). | Không có/sai token -> 401; tenant không active -> 403 'Tổ chức đã bị tạm ngưng'; user inactive/đã xóa -> 401 'User not found or inactive'. | backend/app/routers/auth.py::me + dependencies.py::get_current_user (kiểm tra token, is_active, deleted_at, tenant.status). FE authMe (lib/api.ts:61) gọi sau login/accept-invite/change-password. |


## Nghiệp vụ phát hiện (deepfake image/video, liveness, Grad-CAM, attack type, history, note)

| ID | Chức năng | UC | Actor được phép | Tiền điều kiện | Các bước | Kết quả mong đợi (Pass) | Ca từ chối (RBAC/âm) | Thực trạng (căn cứ) |
|---|---|---|---|---|---|---|---|---|
| TC-DET-01 | Detect ảnh deepfake qua Playground (JWT) — risk-score + verdict | UC-01 | admin, developer (require_role("admin","developer")) | Đã login dashboard (JWT hợp lệ); tenant đang active; role là admin hoặc developer; có 1 ảnh JPG/PNG/WEBP/BMP ≤10MB. | 1. Login dashboard bằng dev@vietbank.vn / Password123! \| 2. Vào trang Playground \| 3. Kéo-thả hoặc click chọn 1 ảnh (hoặc bấm Sample Preset) \| 4. Chỉnh slider Threshold (0-1) nếu cần \| 5. Bấm PHÂN TÍCH \| 6. (hoặc curl) POST /playground/detect/image với header Authorization: Bearer <JWT>, multipart file, query threshold=0.35 | HTTP 200 trả DetectionResponse: risk_score (0-1), risk_band low/medium/high, decision_hint pass/review/reject, thresholds {low:0.30,high:0.70}, verdict REAL/FAKE/UNCERTAIN, confidence, prob_fake, prob_cnn, threshold_used, processing_time_ms, model_version. UI hiện điểm rủi ro ×100, badge band, gauge confidence. Ghi 1 detection source='playground', api_key_id=NULL. | Role compliance/viewer/sysadmin -> 403 (require_role chỉ admin,developer); JWT thiếu/hết hạn -> 401; tenant suspended -> 403; content-type ngoài JPG/PNG/WEBP/BMP -> 400 "Unsupported file type"; ảnh >10MB -> 400 "File size exceeds 10 MB limit". | backend/app/routers/playground.py POST /playground/detect/image (_playground_user=require_role admin,developer). Mặc định MOCK_ML=True và SFDCT_INFER_URL rỗng (config.py) -> chạy _mock_inference (deterministic theo hash) trừ khi cấu hình SFDCT_INFER_URL trỏ microservice :8501. risk từ services/risk.py (RISK_TEMPERATURE=1.0 -> risk_score=prob_fake). FE: frontend/src/components/deepguard/playground-page.tsx + lib/api.ts playgroundDetectImage. |
| TC-DET-02 | Hiển thị Grad-CAM (heatmap vùng nghi vấn) cho ảnh ở Playground | UC-01 | admin, developer | Đang ở Playground; đã chọn ảnh; toggle 'DCT Heatmap' (include_heatmap) đang Bật. | 1. Ở Playground bật toggle DCT Heatmap \| 2. Chọn ảnh và bấm PHÂN TÍCH \| 3. Xem khối '04 Bằng chứng trực quan' và overlay trên ảnh preview \| 4. (curl) POST /playground/detect/image?include_heatmap=true | Nếu microservice SFDCT bật và trả gradcam: field heatmap = data URL base64 PNG/JPEG, UI phủ overlay heatmap thật lên ảnh. Nếu KHÔNG có model thật (mock) hoặc include_heatmap=false: heatmap=null, UI fallback gradient minh hoạ (chỉ khi verdict != REAL) hoặc 'Heatmap không khả dụng'. | include_heatmap=false -> không có Grad-CAM (nhanh ~2x); SFDCT down -> _sfdct_inference fallback mock => heatmap=null. | Grad-CAM CHỈ có thật khi services/ml_inference.py _sfdct_inference gọi serving (params gradcam=true) và serving trả field 'gradcam'; ở chế độ mock mặc định heatmap luôn None. FE playground-page.tsx dòng ~620 ưu tiên result.heatmap, else gradient giả. include_heatmap chỉ là query của route playground (route /v1 không có toggle). |
| TC-DET-03 | Hiển thị phổ tần số 2D-DCT (frequency spectrum) cho ảnh | UC-01 | admin, developer (Playground); External eKYC client (/v1) | Đã detect 1 ảnh (Playground hoặc /v1/detect/image). | 1. Detect 1 ảnh ở Playground \| 2. Xem ô 'Phổ tần số (2D-DCT)' trong khối Bằng chứng trực quan \| 3. (curl) đọc field 'frequency' trong response | field frequency = data URL base64 PNG (log\|2D-DCT\| colormap JET) tính trực tiếp từ ảnh, độc lập với verdict; UI hiện ảnh phổ. Nếu giải mã ảnh lỗi -> frequency=null, UI hiện 'Phổ tần số không khả dụng'. | Ảnh không decode được -> frequency=null; khi xem lại qua GET /v1/results/{id} thì frequency KHÔNG được tái tạo (None). | services/ml_model.py frequency_viz() dùng cv2.dct + applyColorMap (không cần model). Gọi ở cả playground.py và detect.py (detect_image). GET /v1/results/{request_id} trong detect.py KHÔNG trả frequency/heatmap (comment: 'frequency/heatmap không lưu DB -> bỏ qua khi xem lại'). |
| TC-DET-04 | Detect video deepfake qua Playground (JWT) — đồng bộ + lưới frame | UC-02 | admin, developer | Login dashboard role admin/developer; tenant active; có video MP4/WEBM/MOV/MPEG/AVI ≤200MB. | 1. Vào Playground \| 2. Chọn 1 file video \| 3. Bấm PHÂN TÍCH VIDEO \| 4. Xem verdict tổng + lưới Frame Analysis (% fake mỗi frame) \| 5. (curl) POST /playground/detect/video?sample_rate=3 với Bearer JWT | HTTP 200 (đồng bộ) trả VideoDetectionResponse: job_id (UUID sinh tại chỗ, KHÔNG lưu Job DB), verdict, confidence, prob_fake (trung bình các frame), frames_analyzed, frames_fake, frame_results[] (frame_id, prob_fake, thumb base64), model_version, processing_time_ms. UI vẽ lưới frame, badge fake/real theo prob_fake≥0.5. Thiếu thumb -> FE tự trích frame ở client. | Role khác admin/developer -> 403; content-type không thuộc danh sách video -> 400 "Unsupported video type"; video >200MB -> 400 "Video size exceeds 200 MB limit"; inference lỗi -> 400 "Video inference failed: ...". | playground.py POST /playground/detect/video — KHÔNG tạo Job record (khác /v1). Mặc định mock: ml_video.py _mock_inference_video (cắt tối đa 30 frame, trích thumb thật bằng cv2 nếu mở được video). SFDCT bật -> _sfdct_inference_video gửi từng frame sang :8501 (tối đa SFDCT_MAX_FRAMES=60), down -> fallback mock. FE: playground-page.tsx extractFrameThumbs. |
| TC-DET-05 | Detect video deepfake qua API ngoài (/v1) — tạo Job, status 202 | UC-02 | External eKYC client (API-key sk-dg-...) | Có API key hợp lệ của tenant active; còn quota; video hợp lệ ≤200MB. | 1. Tạo API key ở dashboard \| 2. curl POST /v1/detect/video?sample_rate=3 -H 'Authorization: Bearer sk-dg-...' -F file=@video.mp4 \| 3. Đọc response \| 4. (tuỳ chọn) GET /v1/jobs/{job_id} để poll | HTTP 202 trả VideoDetectionResponse kèm job_id THẬT (Job lưu DB, status COMPLETED, progress 100, result JSON). Tăng quota_used của key + tenant usage. GET /v1/jobs/{id} trả status/result. Nếu lỗi: Job.status=FAILED, error_message lưu DB, API trả 400. | API key sai/không tồn tại -> 401 "Invalid API key"; tenant suspended -> 403; vượt quota (quota_used>=quota_limit) -> 429 quota_exceeded; GET /v1/jobs/{id} của tenant khác -> 404 (lọc theo tenant_id). | backend/app/routers/detect.py POST /v1/detect/video tạo Job (JobType.VIDEO_DETECTION) trước, status_code=202; GET /v1/jobs/{job_id} lọc Job.tenant_id==api_key.tenant_id -> not_found nếu khác tenant. Quota tăng qua update ApiKey.quota_used + crud.increment_tenant_usage. |
| TC-DET-06 | Detect ảnh deepfake qua API ngoài (/v1/detect/image) — lưu detection + quota | UC-01 | External eKYC client (API-key) | API key hợp lệ, tenant active, còn quota; ảnh hợp lệ ≤10MB. | 1. curl POST /v1/detect/image?threshold=0.35 -H 'Authorization: Bearer sk-dg-...' -F file=@face.jpg \| 2. Đọc risk_score/verdict/heatmap/frequency \| 3. (tuỳ chọn) GET /v1/results/{request_id} | HTTP 200 DetectionResponse đầy đủ (risk_score, band, decision_hint, thresholds, heatmap, frequency, verdict, prob_fake...). Lưu Detection (source mặc định api, có api_key_id, image_thumb, ip/user-agent). Tăng quota_used + tenant usage. GET /v1/results/{id} trả lại bản ghi (không kèm heatmap/frequency). | Key sai -> 401; tenant suspended -> 403; vượt quota -> 429; loại file sai -> 400; >10MB -> 400; GET /v1/results/{id} của tenant khác -> 404 "Detection result". | detect.py POST /v1/detect/image dùng get_api_key_auth (dependencies.py: quota check, tenant active check). Lưu DB qua crud.create_detection kèm ip_address/user_agent (PII). GET /v1/results lọc theo tenant_id. |
| TC-DET-07 | Passive liveness — 1 ảnh chân dung phân loại LIVE/SPOOF | UC-03 | External eKYC client (API-key); UI dùng API key đang đăng nhập | Có API key (FE liveness-page lấy apiKey từ auth store); tenant active; còn quota; 1 ảnh ≤10MB. | 1. Vào trang Liveness, mode '01. Passive' \| 2. Chọn 1 ảnh chân dung \| 3. Chỉnh slider Ngưỡng LIVE/SPOOF (mặc định 0.125) \| 4. Bấm CHECK LIVENESS \| 5. (curl) POST /v1/detect/liveness?threshold=0.125 -H 'Authorization: Bearer sk-dg-...' -F file=@portrait.jpg | HTTP 200 LivenessResponse: verdict LIVE/SPOOF/UNCERTAIN, liveness_score (0-1), confidence, spoof_type (null nếu LIVE), threshold_used, mode='passive', frame_count=1, processing_time_ms, model_version. UI hiện verdict lớn, score bar, cảnh báo spoof. Lưu 1 LivenessCheck. | FE: thiếu apiKey -> báo 'Chưa có API Key'; thiếu file -> 'Chưa chọn ảnh'. BE: key sai -> 401; tenant suspended -> 403; vượt quota -> 429; loại file sai -> 400; >10MB -> 400. | liveness.py POST /v1/detect/liveness (api_router, get_api_key_auth) -> run_liveness_check. Mặc định LIVENESS_INFER_URL rỗng -> services/liveness.py _mock_liveness (deterministic: ~70% LIVE/20% SPOOF/10% biên). FE liveness-page.tsx detectLivenessPassive (lib/api.ts) gửi apiKey làm Bearer. |
| TC-DET-08 | Active liveness — webcam + challenge gesture, gộp nhiều frame | UC-03 | External eKYC client (API-key); UI webcam | Có API key; tenant active; trình duyệt cho phép webcam. | 1. Trang Liveness chọn mode '02. Active' \| 2. Bấm Bật webcam \| 3. Bấm BẮT ĐẦU CHALLENGE \| 4. Hệ đếm ngược 3s, hiện instruction (vd 'Chớp mắt 2 lần') \| 5. Thực hiện gesture, hệ chụp 5 frame \| 6. (luồng API) GET /v1/liveness/challenge lấy challenge_type rồi POST /v1/detect/liveness/active (files[] + challenge_type + challenge_passed) | GET challenge trả challenge_id, challenge_type, instructions, expires_at (TTL 60s). POST active trả LivenessResponse mode='active', frame_count=số frame, challenge_type, challenge_passed, liveness_score = trung bình các frame. UI hiện Challenge/Passed. Nếu challenge_passed=false -> score bị ép ≤0.25 (thường SPOOF). | files rỗng -> 400 "At least one frame required"; >30 frame -> 400 "Max 30 frames per request"; 1 frame sai type -> 400; frame >10MB -> 400; key sai -> 401; tenant suspended -> 403; FE webcam chưa bật -> 'Hãy bật webcam trước'. | liveness.py GET /v1/liveness/challenge (stateless, challenge_id ngẫu nhiên token_urlsafe) + POST /v1/detect/liveness/active. services/liveness.py _aggregate_frames: avg score, nếu challenge_passed False -> min(avg,0.25), spoof_type = mode phổ biến. FE liveness-page.tsx LƯU Ý: challenge_passed luôn gửi true (mock client, không verify gesture thật). |
| TC-DET-09 | Hiển thị loại tấn công spoof (attack type / spoof_type) ở liveness | UC-03 | admin, developer, compliance, viewer, sysadmin (xem detail); client nhận field qua API | Đã có 1 LivenessCheck verdict SPOOF/UNCERTAIN. | 1. Chạy passive/active liveness ra SPOOF \| 2. Xem khối 'Phát hiện Spoof Attack' trên UI \| 3. (dashboard) GET /liveness/{check_id} đọc spoof_type | spoof_type ∈ {print (Ảnh in giấy), screen (Phát lại màn hình), mask_3d (Mặt nạ 3D), deepfake (Video deepfake), unknown}; null khi LIVE. UI ánh xạ sang nhãn tiếng Việt (SPOOF_LABEL). | Verdict LIVE -> spoof_type=null, UI không hiện cảnh báo. | Enum SpoofType trong DB. Ở chế độ mock _mock_liveness chọn ngẫu nhiên print/screen/mask_3d/deepfake. Ở chế độ thật _real_liveness chỉ trả spoof_type='unknown' khi không LIVE (serving hiện chưa phân loại chi tiết). FE liveness-page.tsx VERDICT_COLOR/SPOOF_LABEL. |
| TC-DET-10 | Danh sách lịch sử phát hiện (history) + filter verdict/ngày | - | admin, developer, compliance, viewer, sysadmin | Login dashboard; tenant active. | 1. Vào trang Lịch sử phát hiện \| 2. Gõ tìm request_id/model/image_hash \| 3. Bấm chip verdict (Real/Fake/Uncertain) \| 4. Chọn khoảng ngày (7/24h/30 ngày/Tất cả) \| 5. Kéo slider Conf ≥ % \| 6. Lật trang \| 7. (curl) GET /detections?verdict=FAKE&start_date=...&page=1&limit=20 | GET /detections trả Paginated[DetectionListItem] lọc theo tenant của user, verdict/start_date/end_date/api_key_id ở server; FE lọc thêm theo confidence + text search trên trang hiện tại. Badge 'Playground' khi source='playground'. Hiện stat strip Tổng/Real/Fake/Conf TB của trang. | JWT thiếu -> 401; tenant suspended -> 403; chỉ thấy detection cùng tenant (cross-tenant không lộ); verdict không hợp lệ -> DetectionVerdict() ném lỗi 500/400. | backend/app/routers/detections.py GET /detections (require_role 5 vai trò) -> crud.list_detections lọc tenant_id. FE history-page.tsx: confidence filter + query search là client-side trên page hiện tại (KHÔNG gọi lại server); date range tính ở dateParams. |
| TC-DET-11 | Xuất CSV lịch sử phát hiện | - | admin, developer, compliance, viewer, sysadmin | Ở trang Lịch sử, có ≥1 bản ghi sau lọc. | 1. Lọc dữ liệu mong muốn \| 2. Bấm nút 'Xuất CSV' \| 3. File CSV tải về máy | Tải file detections_page{N}_{YYYY-MM-DD}.csv gồm cột request_id, created_at, verdict, confidence, prob_fake, model_version, processing_time_ms — CHỈ các bản ghi đã lọc (filtered) của TRANG hiện tại. | Không có bản ghi sau lọc (filtered.length===0) -> nút disabled, không xuất. | Hoàn toàn client-side: history-page.tsx handleExportCsv tạo Blob từ mảng filtered (1 trang, limit=20). Không có endpoint export ở backend; CSV chỉ chứa dữ liệu trang đang xem, không phải toàn bộ. |
| TC-DET-12 | Xem chi tiết một phát hiện (detail) + Forensic Visualizer | - | admin, developer, compliance, viewer, sysadmin | Login dashboard; đã chọn 1 request_id từ trang Lịch sử (cùng tenant). | 1. Ở Lịch sử click 1 dòng \| 2. Trang Chi tiết hiện verdict, prob_fake, score breakdown \| 3. Đổi tab Overlay/Split/Gốc, bật/tắt DCT Heatmap \| 4. (curl) GET /detections/{request_id} | GET /detections/{id} trả DetectionDetail: verdict, prob_fake/prob_cnn/spatial/frequency_score, threshold_used, image_hash, image_thumb (nếu role cho phép), heatmap_url (presigned 1h nếu bật S3, else null), audit_notes[], metadata. UI vẽ overlay/split; nếu không có heatmap_url thật -> overlay gradient mô phỏng khi verdict != REAL. | request_id không thuộc tenant -> 404 "Detection"; JWT thiếu -> 401; tenant suspended -> 403; bản ghi không có image_thumb -> UI hiện 'không có ảnh lưu' (chỉ giữ SHA-256). | detections.py GET /detections/{request_id} -> _load_detection_with_relations lọc tenant_id (chống IDOR cross-tenant). heatmap_url qua storage.presigned_url (None khi S3_BUCKET rỗng -> mặc định không có heatmap thật). FE detail-page.tsx (overlay/split/gradient giả). |
| TC-DET-13 | Che/hiện PII (image_thumb, IP, user-agent) theo role ở detail | - | Hiện: admin, compliance, sysadmin · Che: developer, viewer | Login dashboard; mở chi tiết 1 detection (hoặc liveness check). | 1. Login bằng dev@vietbank.vn (developer) \| 2. Mở chi tiết -> kiểm tra IP/ảnh bị ẩn \| 3. Login lại bằng admin@vietbank.vn (admin) \| 4. Mở cùng bản ghi -> IP/ảnh/user-agent hiện | Role admin/compliance/sysadmin: trả image_thumb, ip_address, user_agent thật. Role developer/viewer: các trường này = null (server mask), UI hiện '—'/ ảnh không khả dụng. | developer/viewer cố đọc PII -> luôn null từ server (không thể bypass ở FE). | detections.py _PII_ROLES={admin,compliance,sysadmin}; _to_detail(mask_pii=role not in _PII_ROLES) gán None cho image_thumb/user_agent/ip_address. Liveness áp dụng tương tự ở liveness.py get_liveness_detail (mask_pii cho image_thumb/ip_address/user_agent). |
| TC-DET-14 | Thêm ghi chú điều tra (audit note) cho một phát hiện | - | admin, compliance (CHỈ 2 role) | Login role admin hoặc compliance; mở chi tiết 1 detection cùng tenant. | 1. Login compliance@vietbank.vn \| 2. Mở chi tiết 1 detection \| 3. Gõ nội dung ghi chú (≤500 ký tự) vào ô 'Audit & review notes' \| 4. Bấm 'Lưu ghi chú' \| 5. (curl) POST /detections/{request_id}/notes body {note} | HTTP 200 trả DetectionDetail cập nhật: thêm 1 phần tử audit_notes {note (cắt ≤500), author_email, author_id, created_at}. Ghi audit log action='detection.note_added'. UI hiện ghi chú mới kèm email tác giả. | Role developer/viewer -> 403 (UI ẩn ô nhập, hiện 'Chỉ admin/compliance được thêm ghi chú'); request_id khác tenant -> 404; JWT thiếu -> 401; note rỗng -> nút disabled ở FE. | detections.py POST /detections/{request_id}/notes dùng require_role("admin","compliance") (SoD: developer tạo detection không tự ghi audit). FE detail-page.tsx canNote = canEdit(role,'detail') = admin+compliance; lib/api.ts detectionsAddNote. |
| TC-DET-15 | Crop & align khuôn mặt MTCNN trước inference (include trong UC-01/02/03) | UC-01 | Model serving SFDCT / backend ML (hệ phụ, nội bộ) | Chạy ở chế độ model THẬT (MODEL_PATH set, MOCK_ML=False) hoặc microservice SFDCT bật. | 1. Gửi ảnh/video qua detect image/video \| 2. Pipeline tự detect mặt MTCNN, crop pad 25%, resize 224 \| 3. Đưa face vào EfficientNet-B4 (TTA flip) + freq score | Ở mode thật (_real_inference): detect mặt bằng MTCNN, crop+pad, nếu không thấy mặt -> chạy toàn ảnh, face_detected=False. Ở mode SFDCT: backend gửi NGUYÊN frame/ảnh, microservice tự crop mặt. prob_fake = 0.75*prob_cnn + 0.25*freq_combined (chỉ mode _real_inference). | Không detect được mặt (mode thật) -> face_detected=false, dùng toàn ảnh; video không mở được -> ValueError -> Job FAILED/400. | services/ml_model.py _crop_face (MTCNN, FACE_PAD_RATIO=0.25, IMG_SIZE=224, USE_TTA=True, FREQ_WEIGHT=0.25) + _predict_face. LƯU Ý: mặc định MOCK_ML=True nên mock KHÔNG crop mặt (face_detected luôn True giả lập); crop thật chỉ khi MODEL_PATH set hoặc SFDCT bật (serving tự crop). |


## Cấp quyền theo API key cho tích hợp (developer/eKYC client)

| ID | Chức năng | UC | Actor được phép | Tiền điều kiện | Các bước | Kết quả mong đợi (Pass) | Ca từ chối (RBAC/âm) | Thực trạng (căn cứ) |
|---|---|---|---|---|---|---|---|---|
| TC-AUTHZ-01 | Developer tạo API key cho tích hợp (POST /api-keys) | - | developer, admin (cùng tenant). KHÔNG cho viewer/compliance; sysadmin có thể nhưng tạo cho tenant của mình. | Đã đăng nhập dashboard (JWT Bearer). Vd dev@vietbank.vn / Password123!, tenant VietBank Demo còn active. | 1) Login POST /auth/login lấy access_token \| 2) Gọi POST /api-keys header Authorization: Bearer <JWT>, body {"name":"eKYC integration","quota_limit":1000,"rate_limit_rpm":60} \| 3) Đọc trường plain_key trong response (chỉ hiện 1 lần) \| 4) Lưu plain_key (dạng sk-dg-...) để gọi /v1/* | 201 Created. Trả id, name, prefix (sk-dg-xxxx...xxxx), status=active, quota_limit, quota_used=0, rate_limit_rpm, plain_key (chuỗi sk-dg-... đầy đủ chỉ trả 1 lần), created_at. Key gắn vào tenant_id của user, KHÔNG mang role. Ghi audit api_key.created. | Role viewer/compliance -> 403 'Requires role: admin, developer' (require_role("admin","developer") trong api_keys.py). Không có/JWT sai -> 401. Tenant suspended -> get_current_user trả 403 'Tổ chức đã bị tạm ngưng.' | backend/app/routers/api_keys.py:19-48 create_api_key, Depends(require_role("admin","developer")); crud.create_api_key (deepguard_db/app/db/crud.py:127-139) sinh plain=sk-dg-<token_urlsafe(32)>, lưu sha256 key_hash + prefix, quota_limit mặc định 1000. plain_key chỉ trả 1 lần. |
| TC-AUTHZ-02 | Gọi phát hiện deepfake ẢNH bằng API key (POST /v1/detect/image) | UC-01 | External eKYC client / developer dùng API-key Bearer (scope theo tenant của key). KHÔNG dùng JWT. | Có plain_key sk-dg-... còn active, tenant active, quota còn (quota_used < quota_limit). | 1) curl -X POST <base>/v1/detect/image -H 'Authorization: Bearer sk-dg-...' -F 'file=@id.jpg;type=image/jpeg' \| 2) (tùy chọn) thêm ?threshold=0.5 \| 3) Đọc verdict/prob_fake/risk_score/risk_band trong JSON | 200 OK. Trả request_id, verdict, confidence, prob_fake, risk_score, risk_band, decision_hint, thresholds, heatmap (Grad-CAM base64), frequency (phổ log\|2D-DCT\| base64), processing_time_ms, model_version. Lưu detection gắn tenant_id + api_key_id của key; tăng quota_used += 1 và tenant current_usage += 1. S3 heatmap/media chỉ lưu khi storage.enabled() (S3_BUCKET có giá trị) - mặc định no-op; CloudWatch metric chỉ bắn khi metrics.enabled() - mặc định tắt. | content_type không thuộc {jpeg,png,webp,bmp} -> 400. File > 10 MB -> 400. Key sai/không gửi -> 401 'Invalid API key'. Key revoked/expired -> validate_api_key trả None -> 401. Tenant suspended -> 403. quota_used >= quota_limit (>0) -> 429 'Quota exceeded for this API key'. Dùng JWT thay vì API key -> 401 (validate_api_key không khớp hash). | backend/app/routers/detect.py:31-126; Depends(get_api_key_auth). Quota check + 429 ở dependencies.py:69-87. Tăng quota: detect.py:93-100. storage.enabled()/metrics.enabled() gate S3+CloudWatch (detect.py:75-90). |
| TC-AUTHZ-03 | Gọi phát hiện deepfake VIDEO bằng API key (POST /v1/detect/video) | UC-02 | External eKYC client / developer dùng API-key Bearer (scope theo tenant của key). | plain_key active, tenant active, quota còn. Video MP4/MOV/AVI/WEBM <= 200 MB. | 1) curl -X POST <base>/v1/detect/video -H 'Authorization: Bearer sk-dg-...' -F 'file=@clip.mp4;type=video/mp4' \| 2) (tùy chọn) ?sample_rate=3 (1..30) \| 3) Lấy job_id trong response \| 4) Poll GET /v1/jobs/{job_id} với cùng key | 202 Accepted (xử lý đồng bộ rồi trả luôn). Trả job_id, verdict, confidence, prob_fake, frames_analyzed, frames_fake, frame_results[], model_version, processing_time_ms. Tạo Job gắn tenant_id + api_key_id; khi xong status=completed result lưu vào job. Tăng quota_used += 1 và tenant current_usage += 1. | content_type không thuộc tập video hợp lệ -> 400. Video > 200 MB -> 400. Key sai -> 401. Tenant suspended -> 403. Vượt quota -> 429. Inference lỗi -> job.status=failed + 400 'Video inference failed'. | backend/app/routers/detect.py:129-218 status_code=202; Depends(get_api_key_auth). Quota tăng detect.py:191-198. Job lưu tenant_id/api_key_id (detect.py:152-159). |
| TC-AUTHZ-04 | Poll trạng thái job video bằng API key (GET /v1/jobs/{job_id}) | - | External eKYC client / developer dùng API-key Bearer (cùng tenant với key). | Có job_id từ /v1/detect/video, key active cùng tenant đã tạo job. | 1) curl <base>/v1/jobs/<job_id> -H 'Authorization: Bearer sk-dg-...' \| 2) Đọc status/progress_percent/result/error_message | 200 OK. Trả job_id, status (processing/completed/failed), progress_percent, result (verdict/prob_fake/frames...), error_message, created_at, completed_at. Query lọc theo tenant_id của key. | job_id của tenant khác (IDOR cross-tenant) -> 404 'Job not found' (filter Job.tenant_id == api_key.tenant_id). job_id không tồn tại -> 404. Key sai -> 401. Tenant suspended -> 403. | backend/app/routers/detect.py:221-244; Depends(get_api_key_auth). select(Job).where(Job.id==job_id, Job.tenant_id==api_key.tenant_id) -> cross-tenant trả None -> not_found. |
| TC-AUTHZ-05 | Lấy lại kết quả detection ảnh bằng API key (GET /v1/results/{request_id}) | - | External eKYC client / developer dùng API-key Bearer (cùng tenant với key). | Có request_id từ /v1/detect/image, key active cùng tenant. | 1) curl <base>/v1/results/<request_id> -H 'Authorization: Bearer sk-dg-...' \| 2) Đọc verdict/prob_fake/risk_score | 200 OK. Trả lại verdict, confidence, prob_fake, risk_score, risk_band, decision_hint, thresholds, processing_time_ms... từ DB. heatmap/frequency KHÔNG lưu DB nên không trả lại khi xem lại (chỉ tái tạo risk từ prob_fake). | request_id của tenant khác (IDOR) -> 404 'Detection result not found' (crud.get_detection lọc theo tenant_id). Không tồn tại -> 404. Key sai -> 401. Tenant suspended -> 403. | backend/app/routers/detect.py:247-280; Depends(get_api_key_auth). crud.get_detection(db, request_id, api_key.tenant_id) -> scope tenant. Comment dòng 257 xác nhận heatmap/frequency không lưu DB. |
| TC-AUTHZ-06 | Gọi liveness thụ động (passive) bằng API key (POST /v1/detect/liveness) | UC-03 | External eKYC client / developer dùng API-key Bearer (scope theo tenant của key). | plain_key active, tenant active. 1 ảnh khuôn mặt <= 10 MB. | 1) curl -X POST <base>/v1/detect/liveness -H 'Authorization: Bearer sk-dg-...' -F 'file=@selfie.jpg;type=image/jpeg' \| 2) (tùy chọn) ?threshold=0.5 \| 3) Đọc verdict (live/spoof)/liveness_score | 200 OK. Trả kết quả liveness (verdict, liveness_score, confidence, spoof_type, threshold_used, mode=passive...). Lưu LivenessCheck gắn tenant_id + api_key_id của key. | content_type không hợp lệ -> 400. File > 10 MB -> 400. Key sai -> 401. Tenant suspended -> 403. (LƯU Ý: dependency get_api_key_auth có check quota nhưng router liveness KHÔNG tăng quota_used/current_usage như detect — vẫn bị chặn 429 nếu quota_used đã >= quota_limit từ trước). | backend/app/routers/liveness.py:57-78 (api_router prefix /v1); Depends(get_api_key_auth). Không thấy lệnh update quota_used trong liveness.py -> liveness không cộng quota (khác detect.py). |
| TC-AUTHZ-07 | Lấy challenge cho liveness chủ động (GET /v1/liveness/challenge) | - | External eKYC client / developer dùng API-key Bearer. | plain_key active, tenant active. | 1) curl <base>/v1/liveness/challenge -H 'Authorization: Bearer sk-dg-...' \| 2) Đọc challenge_id, challenge_type, instructions, expires_at \| 3) Hướng dẫn người dùng làm động tác | 200 OK. Trả challenge_id (token_urlsafe, stateless), challenge_type (blink/turn_left/turn_right/smile/nod ngẫu nhiên), instructions tiếng Việt, expires_at = now + 60s. | Key sai -> 401. Tenant suspended -> 403. Vượt quota (quota_used >= quota_limit) -> 429 (dependency check). Không gửi key -> 401. | backend/app/routers/liveness.py:81-94; Depends(get_api_key_auth). CHALLENGE_TTL_SECONDS=60 (liveness.py:41). challenge_id stateless không lưu DB. |
| TC-AUTHZ-08 | Gọi liveness chủ động (active, nhiều frame) bằng API key (POST /v1/detect/liveness/active) | UC-03 | External eKYC client / developer dùng API-key Bearer. | plain_key active, tenant active. Đã lấy challenge_type. 1..30 frame <= 10 MB mỗi frame. | 1) curl -X POST <base>/v1/detect/liveness/active -H 'Authorization: Bearer sk-dg-...' -F 'files=@f1.jpg;type=image/jpeg' -F 'files=@f2.jpg;type=image/jpeg' -F 'challenge_type=blink' -F 'challenge_passed=true' \| 2) Đọc verdict/liveness_score | 200 OK. Trả kết quả liveness (mode=active, challenge_type, challenge_passed, frame_count...). Lưu LivenessCheck gắn tenant_id + api_key_id của key. | Không có frame -> 400 'At least one frame required'. >30 frame -> 400 'Max 30 frames per request'. frame content_type sai -> 400. frame > 10 MB -> 400. Key sai -> 401. Tenant suspended -> 403. | backend/app/routers/liveness.py:97-126; Depends(get_api_key_auth). Giới hạn 30 frame + 10 MB/frame ở liveness.py:109-118. Lưu qua _save_liveness gắn api_key.tenant_id + api_key.id. |
| TC-AUTHZ-09 | Xác thực & phân tách API key vs JWT (get_api_key_auth) | - | External eKYC client (API-key Bearer trên /v1/*). Tách hoàn toàn với dashboard JWT (get_current_user). | Hệ thống chạy; có ít nhất 1 key active và 1 tenant active. | 1) Gửi key đúng tới /v1/detect/image -> 200 \| 2) Gửi JWT (token dashboard) tới /v1/detect/image -> 401 \| 3) Gửi key tới endpoint dashboard (vd /detections) -> 401/403 (endpoint đó dùng get_current_user, không nhận key) \| 4) So sánh: key không có role, chỉ scope tenant | API key chỉ hợp lệ trên /v1/*; xác thực bằng sha256(plain)==key_hash, status=active, chưa xóa, chưa hết hạn (expires_at NULL hoặc tương lai). Key KHÔNG mang role — quyền truy cập scope theo tenant_id của key (+ quota). JWT chỉ dùng cho dashboard và không validate được như key (hash không khớp) -> 401 trên /v1. | Key hết hạn (expires_at <= now) -> validate_api_key trả None -> 401. Key revoked (status=revoked/deleted_at set) -> 401. Key đúng nhưng tenant.status != active -> 403 'Tổ chức đã bị tạm ngưng.'. Quota cạn -> 429. Dùng JWT trên /v1 -> 401. | backend/app/dependencies.py:69-87 get_api_key_auth dùng _api_key_scheme (HTTPBearer riêng); crud.validate_api_key (crud.py:142-152) lọc ACTIVE + deleted_at NULL + expires_at NULL/tương lai, selectinload tenant. Key không có cột role — ApiKey gắn tenant_id; JWT đi qua get_current_user (dependencies.py:29-66) tách biệt hoàn toàn. |


## Lưu lịch sử/bằng chứng processed lên S3

| ID | Chức năng | UC | Actor được phép | Tiền điều kiện | Các bước | Kết quả mong đợi (Pass) | Ca từ chối (RBAC/âm) | Thực trạng (căn cứ) |
|---|---|---|---|---|---|---|---|---|
| TC-S3-01 | Cổng bật/tắt S3 theo S3_BUCKET (storage.enabled) | - | Cấu hình hệ thống (DevOps/sysadmin đặt biến môi trường), không phải actor đăng nhập | Truy cập file .env / biến môi trường của backend; backend chạy được; biết hành vi mong đợi của API detect. | 1. Đặt S3_BUCKET="" (rỗng) trong .env rồi khởi động backend \| 2. Gọi POST /v1/detect/image (API-key) hoặc playground detect bất kỳ \| 3. Kiểm tra log: không có dòng put_object, không gọi boto3 \| 4. Đổi S3_BUCKET=ten-bucket-that, restart backend \| 5. Gọi lại detect và kiểm tra log/đối tượng trên S3 | S3_BUCKET rỗng: storage.enabled() trả False, mọi nhánh `if storage.enabled()` bị bỏ qua, app chạy y như cũ, KHÔNG import boto3, KHÔNG upload, heatmap_url giữ None. S3_BUCKET có giá trị: enabled() trả True, các nhánh upload được kích hoạt. | - | storage.py:20-22 `enabled()` = `bool(settings.S3_BUCKET)`; config.py:37 mặc định S3_BUCKET="" (tắt). boto3 import muộn trong _client() (storage.py:26) nên môi trường tắt không cần boto3. Mọi call site bọc trong `if storage.enabled()` (detect.py:76,85,171; playground.py:83,91,149). |
| TC-S3-02 | Upload Grad-CAM heatmap lên S3 khi detect ảnh qua API (/v1/detect/image) | UC-01 | External eKYC client (API-key sk-dg-... scope theo tenant); khi BẬT S3 (S3_BUCKET có giá trị) | S3_BUCKET đã cấu hình + credential AWS hợp lệ; có API-key active chưa vượt quota; model serving trả result.heatmap (data URL base64) — chỉ có khi MTCNN dò được mặt/SFDCT sinh Grad-CAM. | 1. curl -H "Authorization: Bearer sk-dg-..." -F file=@anh.jpg http://host/v1/detect/image \| 2. Lấy request_id trong response \| 3. Trên AWS, list s3://<bucket>/tenants/<tenant_id>/detections/ \| 4. Xác nhận có object <request_id>_heatmap.jpg (hoặc .png) \| 5. Truy vấn DB cột detections.heatmap_url của request_id đó | Object heatmap được PUT lên key `tenants/<tenant_id>/detections/<request_id>_heatmap.<ext>` (ext png nếu data URL image/png, ngược lại jpg), ContentType tương ứng; cột detections.heatmap_url lưu S3 KEY (không phải URL công khai). Nếu result.heatmap None (không có Grad-CAM) thì bỏ qua, heatmap_url = None. | API-key sai/thiếu -> 401 (get_api_key_auth); key thuộc tenant suspended -> 403; vượt quota -> 403/429 trước khi tới bước upload; content_type ảnh không hỗ trợ -> 400; ảnh > 10MB -> 400. | detect.py:76-80 `if storage.enabled() and result.heatmap:` rồi `asyncio.to_thread(storage.upload_heatmap, api_key.tenant_id, detection.request_id, result.heatmap)` gán vào detection.heatmap_url. storage.py:30-46 upload_heatmap tách header data URL, base64decode, put_object key `tenants/{tenant_id}/detections/{request_id}_heatmap.{ext}`; DB chỉ lưu key. |
| TC-S3-03 | Upload media gốc (ảnh input) lên S3 phục vụ audit eKYC khi detect ảnh qua API (/v1/detect/image) | UC-01 | External eKYC client (API-key); khi BẬT S3 | S3_BUCKET cấu hình + credential AWS hợp lệ; API-key active chưa vượt quota; gửi ảnh hợp lệ (<=10MB, đúng content_type). | 1. curl -H "Authorization: Bearer sk-dg-..." -F file=@anh.jpg http://host/v1/detect/image \| 2. Lấy request_id \| 3. List s3://<bucket>/tenants/<tenant_id>/detections/ \| 4. Xác nhận có object <request_id>_input.<ext> với đúng đuôi theo content_type (jpg/png/webp) | Bytes ảnh gốc được PUT lên key `tenants/<tenant_id>/detections/<request_id>_input.<ext>`, ext suy từ content_type qua _EXT_BY_CTYPE (mặc định 'bin' nếu lạ), ContentType đặt theo file. Upload chạy trong to_thread, không chặn flow detect. Key media gốc KHÔNG lưu vào DB (chỉ heatmap_url được lưu). | API-key sai -> 401; tenant suspended -> 403; vượt quota -> chặn; ảnh > 10MB hoặc content_type ngoài ALLOWED_IMAGE_TYPES -> 400 (không tới bước upload). | detect.py:85-87 `if storage.enabled(): await _aio.to_thread(storage.upload_media, api_key.tenant_id, detection.request_id, image_bytes, file.content_type or 'image/jpeg', 'input')`. storage.py:55-71 upload_media key `tenants/<tid>/detections/<rid>_input.<ext>`; giá trị trả (key) KHÔNG được gán/lưu DB ở image flow. |
| TC-S3-04 | Upload media gốc (video input) lên S3 phục vụ audit khi detect video qua API (/v1/detect/video) | UC-02 | External eKYC client (API-key); khi BẬT S3 | S3_BUCKET cấu hình + credential hợp lệ; API-key active chưa vượt quota; video hợp lệ (<=200MB, content_type trong ALLOWED_VIDEO_TYPES); job video được tạo. | 1. curl -H "Authorization: Bearer sk-dg-..." -F file=@video.mp4 http://host/v1/detect/video \| 2. Lấy job_id trong response 202 \| 3. List s3://<bucket>/tenants/<tenant_id>/detections/ \| 4. Xác nhận có object <job_id>_input.mp4 | Bytes video gốc được PUT lên key `tenants/<tenant_id>/detections/<job_id>_input.mp4` (ContentType cố định video/mp4, ext mp4 do truyền content_type='video/mp4'). Upload chạy trong to_thread; nếu lỗi S3 không làm fail job. Key không lưu DB. | API-key sai -> 401; tenant suspended -> 403; vượt quota -> chặn; video > 200MB hoặc content_type ngoài ALLOWED_VIDEO_TYPES -> 400; nếu inference lỗi job set FAILED và raise 400 trước khi commit. | detect.py:168-173 `if storage.enabled(): await _aio.to_thread(storage.upload_media, api_key.tenant_id, job.id, video_bytes, 'video/mp4', 'input')`. Lưu ý đặt content_type cứng 'video/mp4' bất kể file thật, key dùng job.id chứ không phải request_id; key không lưu DB. |
| TC-S3-05 | Upload Grad-CAM heatmap lên S3 khi thử nghiệm trong Playground (dashboard) | UC-01 | Người dùng dashboard đã đăng nhập (JWT) có quyền dùng playground (developer/admin/compliance/viewer/sysadmin theo cấu hình route); khi BẬT S3 | S3_BUCKET cấu hình + credential hợp lệ; đăng nhập dashboard (vd dev@vietbank.vn / Password123!); model trả result.heatmap. | 1. Đăng nhập dashboard, mở trang Playground \| 2. Upload 1 ảnh và bấm Detect \| 3. Lấy request_id từ kết quả \| 4. List s3://<bucket>/tenants/<tenant_id>/detections/ \| 5. Xác nhận object <request_id>_heatmap.<ext> + DB detections.heatmap_url lưu key (source='playground') | Heatmap PUT lên `tenants/<tenant_id>/detections/<request_id>_heatmap.<ext>`; detection (source='playground', api_key_id=None) có heatmap_url = S3 key. Nếu result.heatmap None thì bỏ qua. | Chưa đăng nhập/JWT hết hạn -> 401; ảnh không hợp lệ -> 400; (RBAC theo route playground — role không được phép -> 403). | playground.py:83-87 `if storage.enabled() and result.heatmap:` rồi `asyncio.to_thread(storage.upload_heatmap, current_user.tenant_id, det.request_id, result.heatmap)` gán det.heatmap_url. Dùng current_user.tenant_id (JWT), không phải api_key. |
| TC-S3-06 | Upload media gốc (ảnh/video) lên S3 trong Playground (dashboard) | UC-01 / UC-02 | Người dùng dashboard đã đăng nhập (JWT) dùng playground; khi BẬT S3 | S3_BUCKET cấu hình + credential hợp lệ; đăng nhập dashboard; gửi ảnh (<=10MB) hoặc video (<=200MB) hợp lệ. | 1. Đăng nhập, mở Playground \| 2. Detect 1 ảnh -> list S3 thấy <request_id>_input.<ext> \| 3. Detect 1 video -> list S3 thấy <pg_job_id>_input.mp4 \| 4. Đối chiếu key dưới tiền tố tenants/<tenant_id>/detections/ | Ảnh: PUT key `tenants/<tid>/detections/<request_id>_input.<ext>` theo content_type. Video: PUT key `tenants/<tid>/detections/<pg_job_id>_input.mp4` (content_type cứng video/mp4, pg_job_id là uuid4 sinh tại chỗ — KHÔNG có job record DB ở playground video). Key media gốc không lưu DB. | Chưa đăng nhập -> 401; ảnh/video quá kích thước hoặc sai content_type -> 400; video inference lỗi -> 400 (trước upload). | playground.py:91-93 upload_media ảnh dùng det.request_id; playground.py:148-151 video tạo `pg_job_id = uuid.uuid4()` rồi upload_media với content_type='video/mp4'. Cả hai bọc `if storage.enabled()`, chạy to_thread, không lưu key trả về vào DB. |
| TC-S3-07 | Đọc bằng chứng heatmap qua presigned URL hết hạn 1h (chi tiết detection trên dashboard) | - | Người dùng dashboard đăng nhập (JWT): admin/developer/compliance/viewer/sysadmin của tenant | Có detection đã lưu heatmap_url là S3 KEY (tạo lúc S3 bật); đăng nhập dashboard; biết request_id. | 1. Đăng nhập dashboard \| 2. GET /detections/{request_id} (hoặc mở trang chi tiết detection) \| 3. Đọc trường heatmap_url trong response \| 4. Mở URL đó trên trình duyệt trong vòng 1h \| 5. Chờ quá 1h rồi mở lại URL cũ | Khi S3 BẬT và heatmap_url là S3 key: trả presigned GET URL (hết hạn 3600s) mở xem được heatmap; sau 1h URL trả lỗi của S3 (AccessDenied/Expired). Khi S3 TẮT: presigned_url trả None -> heatmap_url=None. Nếu heatmap_url đã là http(s) (legacy) thì trả nguyên không ký lại. | Chưa đăng nhập/JWT hết hạn -> 401; xem detection của tenant khác (IDOR) -> 404 (lọc theo tenant_id trong _load_detection_with_relations); viewer/role thấp xem chi tiết: PII (thumbnail/IP/user-agent) bị mask, nhưng heatmap_url vẫn trả. | detections.py:87 `heatmap_url=storage.presigned_url(d.heatmap_url)`. storage.py:74-90 presigned_url: key rỗng->None, key startswith 'http'->trả nguyên, S3 tắt->None, ngược lại generate_presigned_url get_object ExpiresIn=3600. _load_detection_with_relations (detections.py:58-66) lọc tenant_id => cross-tenant ra 404 (not_found). |
| TC-S3-08 | Khả năng chịu lỗi S3: không raise khi S3 hỏng (upload/presign thất bại không chặn flow) | - | Mọi luồng detect (API-key client hoặc dashboard user); khi BẬT S3 nhưng S3 lỗi (credential sai, bucket không tồn tại, mạng lỗi) | S3_BUCKET cấu hình nhưng credential/quyền/bucket sai để cố tình gây lỗi boto3; backend chạy được. | 1. Đặt S3_BUCKET=bucket-khong-ton-tai hoặc credential sai, restart backend \| 2. Gọi POST /v1/detect/image với ảnh hợp lệ \| 3. Quan sát response code + body \| 4. Kiểm tra log backend \| 5. Mở lại detection và đọc heatmap_url | API detect vẫn trả 200 với đầy đủ verdict/risk/heatmap inline; upload_heatmap/upload_media nuốt exception và trả None (heatmap_url=None trong DB); presigned_url khi lỗi cũng trả None. Chỉ ghi log WARNING, KHÔNG raise, KHÔNG 500. App hoạt động như khi tắt S3. | - | storage.py:44-46, 69-71, 88-90 đều `except Exception as exc: log.warning(...); return None`. _client() dùng lru_cache; lỗi credential/bucket xảy ra lúc put_object/generate_presigned_url nên bị bắt. Call site chỉ gán giá trị trả (None) vào heatmap_url, không có try/except thêm vì service đã đảm bảo không raise. |


## CloudWatch (metrics + request logs)

| ID | Chức năng | UC | Actor được phép | Tiền điều kiện | Các bước | Kết quả mong đợi (Pass) | Ca từ chối (RBAC/âm) | Thực trạng (căn cứ) |
|---|---|---|---|---|---|---|---|---|
| TC-CW-01 | Bắn custom metric mỗi lượt detect ảnh (UC-01) | UC-01 | External eKYC client (API-key) — endpoint POST /v1/detect/image | Backend chạy; CW_METRIC_NAMESPACE đã set (vd DeepGuard/Prod) trong backend/.env; AWS creds (AWS_ACCESS_KEY_ID/SECRET) hợp lệ cho region CW_REGION; API-key sk-dg-... còn quota; tenant active. | 1. Set CW_METRIC_NAMESPACE=DeepGuard/Test + CW_REGION trong backend/.env rồi khởi động backend \| 2. curl -X POST http://localhost:8000/v1/detect/image -H 'Authorization: Bearer sk-dg-...' -F file=@face.jpg \| 3. Vào AWS Console > CloudWatch > Metrics > namespace DeepGuard/Test \| 4. Kiểm tmetric Detections (dimension Source=api), Detections (Source=api + Verdict=<real/fake>), ProcessingLatency, ProbFake | Detect trả 200 bình thường; metrics.enabled()=True nên emit_detection chạy trong thread phụ: put_metric_data đẩy 4 metric (Detections theo Source, Detections theo Source+Verdict, ProcessingLatency ms, ProbFake) lên namespace đã set, Source='api'. Nếu CloudWatch lỗi chỉ log WARNING, KHÔNG chặn response (services/metrics.py emit_detection bọc try/except). | CW_METRIC_NAMESPACE rỗng (mặc định) -> enabled()=False -> KHÔNG gọi emit, không có metric (detect vẫn 200). Creds AWS sai -> put_metric_data raise nhưng bị nuốt, chỉ ghi log WARNING 'CloudWatch put_metric_data failed', flow detect không lỗi. | metrics.py: enabled()=bool(settings.CW_METRIC_NAMESPACE); emit_detection bắn Detections/ProcessingLatency/ProbFake với dim Source(+Verdict), không raise. detect.py:88-90 gọi metrics.emit_detection(..., 'api') qua asyncio.to_thread, chỉ khi metrics.enabled(). config.py:42 CW_METRIC_NAMESPACE='' (tắt mặc định). |
| TC-CW-02 | Bắn custom metric mỗi lượt detect video (UC-02) | UC-02 | External eKYC client (API-key) — endpoint POST /v1/detect/video | Như UC-01; thêm: video <=200MB, content_type thuộc ALLOWED_VIDEO_TYPES; CW_METRIC_NAMESPACE đã set. | 1. Bật CW_METRIC_NAMESPACE như trên \| 2. curl -X POST http://localhost:8000/v1/detect/video -H 'Authorization: Bearer sk-dg-...' -F file=@clip.mp4 \| 3. Xem CloudWatch namespace: Detections/ProcessingLatency/ProbFake với Source=api | Sau khi run_video_inference xong (job COMPLETED), emit_detection được gọi với result['verdict'], result['prob_fake'], processing_ms, source='api' -> đẩy metric lên CloudWatch. latency dùng processing_ms tính từ start_ts. Tắt namespace -> không bắn. | CW_METRIC_NAMESPACE rỗng -> không bắn metric. CloudWatch lỗi -> WARNING, job vẫn COMPLETED. Lưu ý: nếu run_video_inference raise thì code nhảy vào except và KHÔNG bắn metric (emit nằm sau inference) — InferenceErrors KHÔNG được ghi. | detect.py:174-176 gọi emit_detection(result['verdict'], result['prob_fake'], processing_ms, 'api') trong nhánh try sau inference thành công, gated metrics.enabled(). Không truyền error=True nên metric InferenceErrors không bao giờ phát ở luồng này. |
| TC-CW-03 | Bắn custom metric khi detect ảnh qua Playground dashboard | UC-01 | User dashboard có quyền playground (JWT) — POST /playground/detect/image | Đăng nhập dashboard (vd dev@vietbank.vn / Password123!); CW_METRIC_NAMESPACE đã set; tenant active. | 1. Bật CW_METRIC_NAMESPACE \| 2. Login dashboard, vào trang Playground, upload ảnh và bấm Detect \| 3. CloudWatch > namespace > lọc dimension Source=playground | Metric phát với Source='playground' (khác luồng API là 'api'): Detections, Detections+Verdict, ProcessingLatency, ProbFake. Tách Source giúp phân biệt traffic thật (api) vs nội bộ (playground). | CW_METRIC_NAMESPACE rỗng -> không bắn. User không có quyền playground -> bị chặn ở _playground_user trước khi tới đoạn emit (403). CloudWatch lỗi -> chỉ WARNING. | playground.py:94-96 gọi emit_detection(..., 'playground'); playground.py:152-154 cho video cũng source='playground'. Cùng hàm metrics.emit_detection, chỉ khác giá trị dimension Source. |
| TC-CW-04 | Metric InferenceErrors (đếm lỗi suy luận) | - | Hệ thống (tự động) — tham số error của emit_detection | CW_METRIC_NAMESPACE đã set. | 1. Đọc services/metrics.py: tham số error=False mặc định, khi error=True thêm metric InferenceErrors \| 2. grep toàn repo các call site emit_detection \| 3. Xác nhận không call nào truyền error=True | Theo CODE: metric InferenceErrors CHỈ phát nếu gọi emit_detection(..., error=True). Hiện KHÔNG có call site nào truyền error=True (detect.py & playground.py đều chỉ truyền verdict/prob/latency/source) -> trên thực tế metric InferenceErrors KHÔNG BAO GIỜ được bắn. Đây là code dự phòng chưa wiring. | - | metrics.py:45-46 chỉ thêm InferenceErrors khi error=True. Grep emit_detection (detect.py:89/175, playground.py:95/153) cho thấy mọi call chỉ 4 đối số dương, không có error -> InferenceErrors là dead path hiện tại. |
| TC-CW-05 | Ghi log JSON 1 dòng/request vào app.log (REQUEST) | - | Hệ thống — middleware request_logging trên mọi request (trừ SKIP_PATHS) | Backend chạy (middleware luôn bật, KHÔNG cần biến môi trường); file backend/app.log ghi được. | 1. Khởi động backend (setup_request_logging gọi trong main.py) \| 2. Gửi 1 request bất kỳ vd curl http://localhost:8000/v1/detect/image ... \| 3. tail -n 5 backend/app.log \| 4. Kiểm tra dòng 'REQUEST: {...}' chứa request_id, client, method, path, status_code, process_time_ms | Mỗi request (path KHÔNG thuộc {/health,/docs,/openapi.json,/redoc}) sinh 1 dòng INFO 'REQUEST: <json>' trong app.log. Status <500 ghi mức INFO. Đường dẫn /health, /docs... bị bỏ qua, không ghi. | Request tới /health, /docs, /openapi.json, /redoc -> middleware return sớm, KHÔNG ghi dòng REQUEST nào (SKIP_PATHS). | core/request_logging.py: middleware http log_requests, SKIP_PATHS bỏ qua health/docs; ctx có request_id (uuid 12 ký tự), client, method, path, status_code, process_time_ms; logger ghi RotatingFileHandler vào LOG_PATH=backend/app.log (5MB x 3 backup). Bật trong main.py:57-58. |
| TC-CW-06 | Ghi log mức ERROR cho lỗi 5xx / exception (REQUEST_ERROR) | - | Hệ thống — middleware request_logging khi status>=500 hoặc raise | Backend chạy; tạo được tình huống lỗi server (vd DB down làm endpoint trả 500, hoặc một exception chưa bắt). | 1. Gây lỗi 500 (vd tắt DB rồi gọi endpoint cần DB) \| 2. grep ERROR backend/app.log \| 3. Xác nhận dòng 'REQUEST: {...,"status_code":500}' ghi mức ERROR; nếu có exception nổi lên thấy dòng 'REQUEST_ERROR: {...,"error":"..."}' | status_code>=500 -> ghi mức ERROR với prefix 'REQUEST:'. Nếu call_next ném exception -> ghi mức ERROR 'REQUEST_ERROR:' kèm trường error=str(exc) và process_time_ms, rồi re-raise (không nuốt lỗi). Các dòng ERROR này khớp metric filter '"ERROR"' bên CloudWatch để bắn alarm. | Status 2xx/4xx -> ghi INFO 'REQUEST:', KHÔNG phải ERROR (4xx như 401/403/404 không tính là lỗi server). | request_logging.py:41-49: except -> logger.error('REQUEST_ERROR: %s') rồi raise; level = logger.error nếu response.status_code>=500 else logger.info. Comment file nêu rõ status>=500 ghi ERROR để khớp metric filter '"ERROR"'. |
| TC-CW-07 | Forwarder tail app.log đẩy lên CloudWatch Logs (push_to_cloudwatch.py) | - | Hệ thống phụ — systemd unit deepguard-logfwd chạy scripts/push_to_cloudwatch.py | AWS creds hợp lệ trong backend/.env; CW_LOG_GROUP/CW_LOG_STREAM/CW_REGION cấu hình (có default); backend đang ghi app.log; boto3 đã cài. | 1. Chạy: python backend/scripts/push_to_cloudwatch.py (hoặc systemctl start deepguard-logfwd) \| 2. Gửi vài request tới backend để app.log có dòng mới \| 3. Đợi ~5s (POLL_SECONDS) \| 4. AWS Console > CloudWatch > Logs > log group /deepguard/backend > stream ec2-<ngày> \| 5. Kiểm các dòng REQUEST/REQUEST_ERROR đã xuất hiện | Script tạo log group/stream nếu chưa có (nuốt ResourceAlreadyExists), đọc dòng mới từ vị trí lưu ở .logfwd_state.json mỗi 5s, đẩy theo batch <=500 events bằng put_log_events (không dùng sequenceToken). Chịu rotation: nếu file nhỏ hơn vị trí cũ thì đọc lại từ đầu. put_log_events lỗi -> giữ nguyên vị trí cũ, log WARNING, thử lại vòng sau (không mất log). | AWS creds thiếu/sai -> boto3 client('logs') hoặc put_log_events ném lỗi; create group/stream lỗi không phải ResourceAlreadyExists sẽ raise (script dừng). app.log chưa tồn tại -> _read_new_lines trả rỗng, không đẩy gì. Đây là tiến trình NGOÀI app, không gắn metrics.enabled() — chạy độc lập bằng systemd. | scripts/push_to_cloudwatch.py: POLL_SECONDS=5, MAX_BATCH=500, _load_pos/_save_pos qua .logfwd_state.json, _ensure_group_stream nuốt ResourceAlreadyExists, _read_new_lines xử lý rotation (size<pos->pos=0), put_log_events lỗi -> new_pos=pos + WARNING. LOG_GROUP default /deepguard/backend, stream ec2-<YYYY-MM-DD>. |


## Analytics & Audit log

| ID | Chức năng | UC | Actor được phép | Tiền điều kiện | Các bước | Kết quả mong đợi (Pass) | Ca từ chối (RBAC/âm) | Thực trạng (căn cứ) |
|---|---|---|---|---|---|---|---|---|
| TC-ANA-01 | Analytics Overview — KPI tổng quan (total/fake/real/uncertain, fake_rate, avg & p95 latency) | - | sysadmin, admin, compliance, developer, viewer (mọi role đã đăng nhập, trong cùng tenant) | Đã đăng nhập (JWT Bearer), tenant đang active, có sẵn dữ liệu Detection trong tenant. Demo: admin@vietbank.vn / Password123! (tenant VietBank Demo). | 1. Đăng nhập dashboard lấy access_token \| 2. Mở trang Analytics (analytics-page.tsx) hoặc gọi GET /analytics/overview?days=30 (kèm header Authorization: Bearer <token>) \| 3. Quan sát dải KPI: Tổng requests, Deepfake rate (%), P95 Latency (ms), Avg Latency (ms), Fake detected \| 4. Đổi tham số days=7 để xem 7 ngày qua | HTTP 200 trả AnalyticsOverview gồm total_requests, fake_detected, real_detected, uncertain, fake_rate (làm tròn 2 chữ số), avg_latency_ms, p95_latency_ms, days. Số liệu được tính TỪ bảng Detection lọc theo tenant_id của user và created_at >= now-days (crud.analytics_overview). KPI hiển thị đúng các trường này; nếu chưa có dữ liệu thì các giá trị = 0, fake_rate = 0.0. | Không có token / token sai → 401 (get_current_user). Tổ chức bị suspended (tenant.status != active) → 403 'Tổ chức đã bị tạm ngưng'. Cross-tenant: query luôn ép Detection.tenant_id == current_user.tenant_id nên không xem được dữ liệu tenant khác. days ngoài [1,365] → 422. | Endpoint GET /analytics/overview (backend/app/routers/analytics.py L19-26), require_role cho cả 5 role kể cả viewer/developer. Tính toán thật trong deepguard_db crud.analytics_overview (L245-275): dùng func.percentile_cont(0.95) (Postgres) cho p95, func.avg cho avg, đếm theo DetectionVerdict. avg/p95 ép int(). Schema AnalyticsOverview ở backend/app/schemas/analytics.py. |
| TC-ANA-02 | Analytics — biểu đồ phân bố kết quả (bar chart + Donut Real/Fake/Uncertain) | - | sysadmin, admin, compliance, developer, viewer (cùng tenant) | Đã đăng nhập, tenant active, overview đã tải xong. | 1. Mở trang Analytics \| 2. Cuộn tới khối 'Phân bố kết quả phát hiện' \| 3. Xem cột bar (Real/Fake/Không chắc chắn) và Donut với tổng ở giữa \| 4. Đổi range 7d/30d để thấy số cập nhật | Hiển thị 3 phân khúc dùng overview.real_detected / fake_detected / uncertain (cùng dữ liệu overview, KHÔNG có endpoint riêng). verdictTotal = tổng 3 giá trị. Nếu verdictTotal = 0 → hiện EmptyState 'Chưa có dữ liệu phát hiện' thay vì chart. | Không có dữ liệu detection → EmptyState (không lỗi). RBAC/suspended giống Overview (kế thừa cùng call /analytics/overview). | Chỉ là render client từ AnalyticsOverview (analytics-page.tsx L181-368). Dùng InteractiveChart mode='bar' + Donut từ shared. KHÔNG có dữ liệu time-series theo ngày (chỉ 1 cột tổng hợp cho cả khoảng), axis=[rangeLabel]. |
| TC-ANA-03 | Analytics — phân phối độ trễ (Latency: Avg, P50, P95, P99) | - | sysadmin, admin, compliance, developer, viewer (cùng tenant) | Đã đăng nhập, tenant active, overview tải xong. | 1. Mở Analytics \| 2. Xem khối 'Độ trễ (Latency)' ở hàng dưới \| 3. Quan sát 4 hàng Avg / P50 / P95 / P99 | Chỉ Avg (avg_latency_ms) và P95 (p95_latency_ms) hiển thị giá trị thật kèm thanh bar; P50 và P99 hiển thị nhãn 'chưa hỗ trợ' và thanh trống. | - | backend chỉ trả avg + p95 (crud.analytics_overview). UI (analytics-page.tsx L207-215, L399-407) hard-code P50/P99 value=null, real=false → render 'chưa hỗ trợ'. Đây là honest empty, không bịa số. |
| TC-ANA-04 | Analytics — Usage / hạn mức tháng (đã dùng / hạn mức / còn lại + % dùng) | - | sysadmin, admin, compliance, developer, viewer (cùng tenant) | Đã đăng nhập, tenant active. | 1. Đăng nhập \| 2. Mở Analytics (gọi kèm GET /analytics/usage) hoặc curl GET /analytics/usage với Bearer token \| 3. Xem 3 ô StatPill: 'Đã dùng tháng này', 'Hạn mức tháng', 'Còn lại (… % dùng)' | HTTP 200 trả UsageInfo: monthly_quota, current_usage, remaining = max(0, quota-used), usage_percent = round(used/quota*100,2) (0.0 nếu quota=0). Lấy từ tenant.current_usage và tenant.monthly_quota của chính tenant user. Ô 'Còn lại' đổi màu cảnh báo khi usage_percent >= 80%. | Không token / sai → 401. Tổ chức suspended → 403. Chỉ đọc usage của tenant mình (crud.get_tenant theo current_user.tenant_id), không cross-tenant. | GET /analytics/usage (analytics.py L29-42), require_role cả 5 role. Frontend analyticsUsage() (api.ts L261). UI ở analytics-page.tsx L255-284. Lỗi tải bị nuốt (.catch(()=>null)) nên ô usage chỉ ẩn nếu null, không hiện thông báo lỗi. |
| TC-ANA-05 | Analytics — Theo API key (per-key usage) | - | sysadmin, admin, compliance, developer, viewer (cùng tenant) | Đã đăng nhập, mở trang Analytics. | 1. Mở Analytics \| 2. Xem khối 'Theo API key' ở hàng dưới cùng | Luôn hiển thị EmptyState 'Chưa có dữ liệu theo key' với ghi chú 'Thống kê usage theo từng API key sẽ sớm khả dụng'. KHÔNG có số liệu. | - | Tính năng SẮP CÓ — không có trường backend nào. analytics-page.tsx L425-441 hard-code EmptyState, không gọi API. |
| TC-ANA-06 | Analytics — Phân bố khu vực địa lý (geo distribution) | - | sysadmin, admin, compliance, developer, viewer (cùng tenant) | Đã đăng nhập, mở trang Analytics. | 1. Mở Analytics \| 2. Xem khối 'Phân bố khu vực' | Luôn hiển thị EmptyState 'Sắp ra mắt' với ghi chú 'Phân tích theo khu vực địa lý đang được phát triển'. KHÔNG có dữ liệu/bản đồ. | - | Tính năng SẮP CÓ — không có endpoint/trường backend. analytics-page.tsx L443-458 hard-code EmptyState. |
| TC-ANA-07 | Audit Logs — liệt kê nhật ký (list + phân trang) | - | admin, compliance, sysadmin (CHỈ 3 role này) | Đã đăng nhập bằng admin/compliance/sysadmin, tenant active, có bản ghi AuditLog trong tenant. Demo: compliance@vietbank.vn / Password123!. | 1. Đăng nhập role admin/compliance/sysadmin \| 2. Mở trang Audit Logs hoặc GET /audit-logs?page=1&limit=50 với Bearer token \| 3. Xem bảng/timeline: thời gian, action, resource, user, IP \| 4. Click một dòng để bung metadata, resource_id, user_agent, severity \| 5. Chuyển trang bằng nút phân trang | HTTP 200 trả Paginated[AuditLogItem] (items, total, page, limit) sắp theo created_at desc, lọc theo tenant_id của user, join User để lấy user_email. Mỗi item gồm id, action, resource_type, resource_id, user_id, user_email, metadata, ip_address, user_agent, created_at. UI hiện 50 dòng/trang. | developer hoặc viewer → 403 'Requires role: admin, compliance, sysadmin'. Không token / sai → 401. Tổ chức suspended → 403. Cross-tenant: where AuditLog.tenant_id == current_user.tenant_id nên không thấy log tenant khác (không có IDOR). limit > 200 hoặc < 1, page < 1 → 422. | GET /audit-logs (backend/app/routers/audit.py L32-77), require_role('admin','compliance','sysadmin') → developer/viewer bị chặn. Join User isouter để map email (audit.py L43-45). Frontend auditLogsList() (api.ts L234-247), audit-page.tsx. 'severity' và icon là SUY LUẬN ở frontend từ action (audit-page.tsx L8-63), backend KHÔNG có trường severity. |
| TC-ANA-08 | Audit Logs — lọc theo action / resource_type / khoảng thời gian | - | admin, compliance, sysadmin | Đã đăng nhập role hợp lệ, tenant active. | 1. Mở Audit Logs \| 2. Chọn dropdown 'action' (vd api_key.revoked) \| 3. Chọn dropdown 'resource' (vd detection) \| 4. Chọn khoảng thời gian (24 giờ / 7 ngày / 30 ngày / Tất cả) \| 5. Hoặc curl GET /audit-logs?action=user.login&resource_type=user&start_date=...&end_date=... | Backend lọc server-side: action (==), resource_type (==), created_at >= start_date, created_at <= end_date; total và items phản ánh đúng filter. UI gửi start_date/end_date ISO theo lựa chọn 'Tất cả' = bỏ qua filter ngày. Đổi filter reset về trang 1. | developer/viewer → 403. Không token → 401. start_date/end_date sai định dạng datetime → 422. | Filter server-side trong audit.py L47-54. UI dropdown ACTION_OPTIONS/RESOURCE_OPTIONS và DATE_RANGES (audit-page.tsx L69-77, L201-207). Lưu ý: ô search free-text ('Tìm action, user, IP') chỉ lọc CLIENT-SIDE trên trang hiện tại (audit-page.tsx L227-236), không gửi lên server. |
| TC-ANA-09 | Audit Logs — xuất CSV | - | admin, compliance, sysadmin | Đã đăng nhập role hợp lệ, trang audit đang có ít nhất 1 bản ghi đã tải. | 1. Mở Audit Logs \| 2. Áp filter mong muốn để có dữ liệu \| 3. Bấm nút 'Xuất CSV' | Tải về file audit_logs_<YYYY-MM-DD>.csv chứa các cột id, created_at, action, resource_type, resource_id, user_email, ip_address — CHỈ các dòng của TRANG hiện tại (tối đa 50), không phải toàn bộ total. Nút bị disable khi rows.length === 0. | Không có dữ liệu trang hiện tại → nút disabled (không tạo file). | Xuất hoàn toàn CLIENT-SIDE bằng Blob (audit-page.tsx L266-283), KHÔNG có endpoint export backend; chỉ chứa dữ liệu đã fetch của trang hiện tại nên không gồm metadata/user_agent và không gom hết total. |
| TC-ANA-10 | Audit Logs — RBAC chặn developer/viewer truy cập | - | Chặn: developer, viewer (và anonymous) | Đăng nhập bằng dev@vietbank.vn hoặc viewer@vietbank.vn / Password123!. | 1. Đăng nhập role developer hoặc viewer \| 2. Gọi GET /audit-logs với token đó (hoặc thử mở trang Audit qua menu) | HTTP 403 'Requires role: admin, compliance, sysadmin'. Trang audit hiển thị state lỗi (audit-page.tsx bắt error và hiện thông báo + nút Thử lại); menu Audit thường bị ẩn theo RBAC client. | Đây chính là ca RBAC âm: developer → 403; viewer → 403; anonymous (không token) → 401. | require_role('admin','compliance','sysadmin') (audit.py L40) so sánh current_user.role.value (dependencies.py require_role L96-109, forbidden = 403). Khác với Analytics cho cả 5 role — Audit hẹp hơn đúng yêu cầu compliance. |
