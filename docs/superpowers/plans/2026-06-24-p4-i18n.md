# P4 — i18n EN/VIE (foundation) (Plan)

**Goal:** Nền i18n EN/VIE chạy được + demo được cho dashboard + Streamlit, mặc định vi.
**Branch:** `p4-i18n` → merge `dev-thanhln-24062026`.

## Infrastructure
- **`frontend/src/store/locale.ts`** (mới): Zustand persist `dg-locale`, default `vi` (theo pattern appearance.ts).
- **`frontend/src/lib/i18n.ts`** (mới): `DICT` (73 key) + `useT()` → `t(key)` resolve theo locale.
- **Streamlit** (`ekyc_demo/app.py`): `T` dict + `tr()` + `st.session_state["locale"]`.

## Toggle (3 nơi)
- Top-header: nút VI/EN luôn hiện (mọi trang).
- Settings: panel ngôn ngữ.
- Streamlit: radio "🌐 VI/EN" ở sidebar.

## Coverage (BOUNDED — chủ ý)
- **Phủ:** shell (sidebar nav labels + group titles + top-header title/sub mọi trang in-scope) + login page (đầy đủ) + Streamlit (sidebar, 3 mode, nút chạy, tiêu đề kết quả, card title).
- **CHƯA phủ:** body các trang sâu (history/detail/models/team/account/...) — theo cùng pattern `useT()`/`t('key')`, là follow-on mechanical. Shell + header switch ngôn ngữ là phần nhìn rõ nhất khi demo.

## Locale-safe Streamlit
`mode_key` (English ổn định theo index radio) tách khỏi label hiển thị đã dịch → logic `if mode_key=="Liveness"`/`"Deepfake"`/else-cascade KHÔNG vỡ khi đổi ngôn ngữ.

## Verify
- `tsc --noEmit`: **0 lỗi** toàn frontend.
- Streamlit `py_compile` OK + load :8503.
- *Eyeball: bấm toggle VI/EN ở top-header → nav + tiêu đề đổi ngôn ngữ; Streamlit sidebar VI/EN.*

## Commit
`feat(i18n): EN/VIE foundation (locale store + dict + toggle) — shell/login/streamlit`. No co-author.
