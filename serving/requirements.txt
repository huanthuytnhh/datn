# serving/requirements.txt — deps cho 2 inference server (SFDCT :8501 + liveness :8502)
# Đã smoke-test OK với các version ghi chú bên phải (Python 3.10, 2026-06-11).
# EC2 CPU-only: cài torch bản CPU cho nhẹ:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2                  # tested 2.6.0
torchvision>=0.17           # tested 0.21.0
efficientnet_pytorch==0.7.1
opencv-python-headless>=4.8 # tested 4.11
numpy>=1.26                 # tested 2.2.6
PyYAML>=6.0
scikit-learn>=1.3           # tested 1.7.2
tensorboard>=2.16           # detector import SummaryWriter; >=2.16 mới tương thích numpy 2.x
kornia>=0.7                 # tested 0.8.2 (loss/__init__ kéo multi_attention_detector)
pillow>=10.0
fastapi>=0.110              # tested 0.115
uvicorn>=0.29               # tested 0.32
python-multipart>=0.0.9     # FastAPI UploadFile
